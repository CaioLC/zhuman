# Product roadmap

**The only document that discusses what is not built.** Gaps, limitations, sequencing, deferred ideas, and arguments about future work belong here and nowhere else. Present-tense product rules live in [`design.md`](design.md) and [`ui_design.md`](ui_design.md); implemented architecture lives in [`../src/README.md`](../src/README.md), [`../src/ui/README.md`](../src/ui/README.md), and [`../src/ui_client/README.md`](../src/ui_client/README.md).

This roadmap preserves the pre-existing simulation, economics, content, later-act, engine, performance, and tooling work while mapping the finalized Act I/II HTML prototypes exhaustively onto the Zig/SDL product. Prototype parity is the immediate delivery focus, not permission to erase unrelated future work.

Work is split below by architectural responsibility and tied back together by the dependency spine. `src/ui/` remains a generic, extractable immediate-mode engine; `src/ui_client/` binds that engine to SDL; `src/pages/templates/` owns game-specific reusable compositions; `src/pages/` owns screens and applies user intent to the simulation; simulation modules own authoritative rules and atomic state changes.

## 0. Contract for using the prototypes

### Source precedence

When two references disagree, use this order:

1. `prototypes/act-i.html` and `prototypes/act-ii.html` as the rendered-product and interaction truth, including their current embedded scripts.
2. `prototypes/prototype.css` for exact visual tokens, density, state styling, breakpoints, clipping, and transition timing.
3. `prototypes/UNIFIED_UI_SPEC.md` for cross-act component identity, vocabulary, and normalization decisions.
4. `docs/hex_tech_board_spec.md` for the canonical Act II board topology, coordinates, technology roster, migration rules, and board invariants.
5. Screenshots in `prototypes/` as visual regression fixtures. Screenshots with older merchant wording or the obsolete direct-barter Act II exchange do not override current HTML.
6. `docs/design.md` and `docs/ui_design.md` for game rules outside the rendered prototype. An unresolved economic rule remains a blocker or fixture-backed interface; UI code must not silently invent it.

The final prototype commit deliberately removed the visible `blocked` recipe state, normalized the merchant modal, adopted query-and-sort BUILD in both acts, made milestone cards collapsible, and established per-view Holdings collapse. Dead CSS or stale screenshot text is not a feature request. HTML is authoritative for rendered behavior; the explicit cross-act economic and topology invariants below override fixture-data drift. In particular, the unified `1:1` rule makes the Act I `12m` versus Act II `8 biomass` Garden bed mismatch a bug: normalize the shared recipe to `8` rather than preserving two prices.

### Native translation rules

The implementation is not a browser port and must not grow a CSS or DOM interpreter.

- CSS selectors become explicit Zig template state and style fragments.
- CSS grid layouts become the smallest required engine sizing primitive plus ordinary row/column composition. A general web-grid engine is out of scope.
- Media queries become frame-local `ViewMetrics` and explicit compact/narrow template branches.
- ARIA becomes keyboard behavior, visible focus, semantic metadata, and an accessibility bridge where the platform supports one.
- URL hashes/query parameters are prototype review controls, not player-facing desktop requirements. Equivalent deterministic states belong in a development fixture/deep-link harness; normal tab, query, sort, disclosure, camera, and collapse state persists in keyed UI/session state.
- Native HTML controls (`dialog`, `search`, `select`, `range`) become reusable SDL controls with equivalent pointer, keyboard, focus, disabled, and dismissal behavior.
- Prototype JavaScript that mutates balances is illustrative wiring. Production UI calls authoritative simulation operations and never completes half of a trade, build, research payment, or act transition locally.

### Non-negotiable product invariants

- Act I has Vigor, Food, and generic Materials; Act II has Vigor, Population, Coin, Food, Water, Fuel, Metal, Minerals, and Biomass.
- At the Act I curtain, Food carries unchanged and unspent Materials converts to Biomass at exactly `1:1`. Built goods remain built. Same-named Act I material costs and Act II biomass costs remain numerically equal.
- Energy is a flow/rate in the runline, never a stockline balance.
- Act I has ACTIONS and BUILD. Act II adds STRUCTURE.
- Act I ACTIONS is a compact static roster; Act II ACTIONS is a 20-entry searchable/sortable catalog.
- BUILD uses one data-generated six-column recipe catalog in both acts. Impossible/prerequisite-locked recipes are absent, not grey `blocked` rows. Newly eligible recipes appear with an event-log announcement.
- Recipe states visible to the player are `ready`, `reach`, `owned`, and `building`; milestone states are `locked`, `unfunded`, `ready`, and `done` (`built`/`standing` copy is act-specific).
- Shelter and Exchange share one milestone component. Passerby, Merchant, and Exchange share one market-strip/trade-dialog skeleton with identity-specific wording.
- Holdings collapse is remembered separately for ACTIONS and BUILD. Entering STRUCTURE forces collapse each time without overwriting those memories.
- ACTIONS and BUILD retain the four-line event log. STRUCTURE alone hides it.
- The Act II board contains one noninteractive ring-0 foundation plus exactly 60 technology tiles in rings 1–4. It does not reserve Act III rings.

### Repository constraints

- [ ] **BASE-01 — Keep all layer boundaries enforceable.** `src/ui/` imports no SDL, game, theme value, or `Resources` field; `src/ui_client/` imports only the engine and SDL-facing resources; templates/screens may know game vocabulary; simulation modules know no UI types.
- [ ] **BASE-02 — Keep ECS reflection files pure.** `src/components.zig` and `src/tags.zig` contain only public component/tag struct declarations. Helpers, enums not intended as components, catalog metadata, quote types, and algorithms live in non-reflected modules or private declarations.
- [ ] **BASE-03 — Preserve the green baseline after every implementation slice.** Required commands are `zig build test-ui`, `zig build test`, and `zig build`. Add a new focused build step only when it shortens a real loop; do not weaken an existing target to make it pass.
- [ ] **BASE-04 — Update present-tense documentation with each landed capability.** A finished engine/client change updates the matching README in the same slice and removes its future entry here. Do not leave stale “not built” comments in code.
- [ ] **BASE-05 — Give every repeated row/tile a stable domain ID.** Node keys must derive from stable action/recipe/technology/offer IDs, not current sort position, translated display text, pointer address, or filtered index. Selection, focus, scroll anchors, animation, and cached textures must survive reorder/filter operations.
- [ ] **BASE-06 — Add debug duplicate-key diagnostics.** In debug builds, detect duplicate sibling/path keys during frame construction and report the full structural path before pooled state aliases silently. Cover loops and conditionally inserted children.

## 1. Current baseline to retain

Do not replace working mechanisms merely because the HTML used a browser feature.

- The ephemeral arena tree, structural Wyhash keys, typed keyed pools, pruning/eviction, and handle-not-pointer rule remain the persistence model.
- Last-frame geometry, paint-order hit testing, topmost occlusion, clip-aware rejection, pass-through probes, and ancestor bubbling remain the interaction foundation.
- Fixed/content/percent/fit sizing, row/column flow, wrapping, reverse order, main-axis distribution, cross-axis alignment, baseline alignment, gap, padding, anchors, offsets, scroll translation, and overflow clipping remain the layout foundation.
- Multi-root rendering remains the overlay/layering model.
- The feature registry and `RenderData` conformance check remain the rendering extension seam.
- `El`, content leaves, style fragments, game templates, and direct inline intent handling remain the composition model.
- Existing action/build affordability, distribution statistics, `Busy` progress, build cancellation/refund, good counts, spare-first breaking, log storage, and the basic Shelter food/goods gates are starting points rather than work to recreate.

## 2. Dependency spine and delivery slices

Implement and validate vertical slices in this order. A later slice may use deterministic fixture data before its simulation backend is complete, but fixture-only behavior is not product completion.

1. **Foundation safety:** BASE tasks, input/focus, text, responsive metrics, remaining-space sizing, geometry, and reusable overlay/scroll controls.
2. **Shared visual language:** tokens, shell, header, rail, navigation, strips, catalog controls, rows/tiles, Body, milestone, trade modal, and log.
3. **Act I parity:** static actions, live BUILD, continuous eating, live Shelter, passerby barter, responsive shell, and transition migration.
4. **Act II shell/catalog parity:** typed HUD, population/coin, searchable actions, 50-recipe-capable BUILD, coin merchant, and Exchange lifecycle.
5. **STRUCTURE:** hex math, geometry, shape hits, research state, detail, camera, filters/search, keyboard, and BUILD/Exchange synchronization.
6. **Hardening:** screenshot matrix, keyboard-only journeys, semantic bridge, performance/leak checks, cleanup, and stale-path removal.

Critical dependencies:

- Persistent Holdings requires `VIEW-01`, `UIE-03`, and `KIT-05`.
- Search/sort and trade dialogs require `INPUT-01..07`, `TEXT-01..04`, and `KIT-08`/`KIT-15`.
- Honest distribution curves and board tiles require `RENDER-03`.
- Board selection requires `UIE-05`; board panning requires `INPUT-05`; board keyboard navigation requires `UIE-04` and `INPUT-06`.
- Act I trade UI requires authoritative encounter state, quotes, atomic barter, marginal pricing, and supersession (`ACT1-12..16`), not just a modal.
- Live Act II UI requires typed ledgers/catalog snapshots (`ACT2-01..07`). Board chrome can be fixture-built earlier, but research completion cannot ship against prototype-local balances.

## 3. Generic engine work — `src/ui/`

### Persistent state and identity

- [x] **UIE-01 — Honor state initialization rather than blindly zeroing every type.** Extend `Pool(T)` with an explicit initialization convention (`T.init`, a supplied initializer, or a documented zeroable trait). Preserve zero initialization for truly zeroable states. Add tests proving nonzero enum/default values initialize correctly, holes reuse safely, and resource-owning states still deinitialize exactly once.
  - COMMENT: Implemented with a no-argument `T.init() T` hook plus a documented zeroable fallback. Fresh slots and reused holes share the same initialization path; focused tests cover semantic defaults, dirty-hole reuse, and exactly-once resource cleanup.
- [x] **UIE-02 — Add optional persistence policy for conditionally hidden UI state.** Per-view query/sort/scroll/collapse state must not disappear merely because its subtree is not built for one tab. Prefer one always-built shell state containing all view state; if the pool gains retention/grace frames, keep that mechanism generic, bounded, and tested. Do not retain whole trees.
  - COMMENT: Added non-allocating per-frame retention for existing typed slots. Always-built owners can retain a hidden direct child's state by structural key; stopping the request restores immediate pruning. BUILD now owns its view state on its conditional root, retained by the center shell while ACTIONS is active. Tests cover missing-key no-allocation, survival while retained, and pruning when retention stops.

### Layout and responsive composition

- [x] **UIE-03 — Add the minimum remaining-space and constraint sizing needed by the shell.** Support a child that consumes available main-axis space beside fixed siblings, plus per-axis minimum/maximum bounds. Define deterministic overflow/shrink behavior and preserve the current pure, callback-free solve. Required cases: `252px rail + gap + flexible surface`, `36px collapsed rail + flexible surface`, dialog side columns plus a flexible preview, and a viewport whose header/footer stay fixed while the result collection fills the remainder.
  - Test nested fixed/flexible rows and columns, padding, wrap, min/max clamping, zero remaining space, and multiple grow children.
  - Do not implement a CSS grid parser; synchronized catalog columns may remain a template concern.
  - COMMENT: Added `SizeRule.grow` for equal-share definite-parent main-axis remainder and content-box min/max bounds on both axes. Grow allocation is min-first, redistributes max-capped shares, preserves fixed/min extents on overflow, accounts for padding and packed gaps, and falls back to measured content when no usable parent remainder exists. The pure top-down pass allocates before descending so nested percentages see final flexible sizes; focused tests cover the required compositions and edge cases.
- [x] **UIE-04 — Add a generic focus registry and lifecycle.** A frame can register focusable keys in traversal order, identify the focused key, move next/previous, and prune or repair focus when a key disappears or becomes disabled. Keep focus singular and key-based. Support roving groups so tabs and the board can own directional navigation without every tile entering global Tab order.
  - Test focus removal, disabled targets, wrap/no-wrap traversal, conditional subtrees, and stable focus after reorder by domain key.
  - COMMENT: Added a generic double-buffered stable-key focus registry: event-stage traversal reads the last completed order while the build registers the next. Singular focus survives reorder by key, skips disabled targets, supports wrapped or bounded next/previous movement, and repairs by prior ordinal when targets disappear, disable, or conditional trees close. Roving groups retain one enabled active member as their sole global Tab stop while exposing directional member traversal; absent groups are pruned. Text input now registers and routes through the lifecycle API, with pure tests covering removal, disable, empty trees, reorder, traversal boundaries, and roving repair.
- [x] **UIE-05 — Add shape-aware hit testing.** Extend an interaction slot with optional host-supplied hit logic or equivalent local-shape metadata. `mark` still applies clip, paint order, pass-through, and ancestor bubbling, but rejects points outside the shape before claiming the hit.
  - The generic engine must not know what a hexagon is.
  - Test that transparent hex-bounding-box corners fall through to the correct neighbor/background and that a clipped shape cannot hit outside its viewport.
  - COMMENT: Added a generic frame-scoped `HitTestFn(Rect, x, y)` declaration on interaction slots and a fluent host `El.hit_test` binding. `mark` preserves reverse paint order, pass-through, inherited clipping, rectangular broad-phase rejection, and ancestor bubbling; a predicate rejection continues to lower slots. Predicates are static host policy, automatically expire when omitted on the next build, and carry no hex/game knowledge in production core. Pure tests use a test-only flat-hex predicate to prove transparent corners reach the overlapping neighbor or background, clipped shapes cannot claim outside their viewport, and existing rectangular semantics remain intact.
- [x] **UIE-06 — Add pointer capture by key.** Once a host captures a pointer for a key, motion/release is routed to that key even outside its last rect until explicit release, cancellation, disappearance, or focus loss. Capture must coexist with topmost hit testing and must be pruned safely.
  - Test slider drag, scrollbar drag, board pan, release outside, disappearing capture owner, and no click-through after a captured drag.
  - COMMENT: Added singular stable-key pointer capture to generic `Ctx`. Capture accepts only a live interaction slot; while owned, `mark` routes every host-defined pointer flag directly to that key and its stamped ancestors before coordinate, pass-through, clip, rectangle, or shape checks, preventing outside drags/releases from clicking through. Owner-checked release, unconditional host cancellation/window-focus-loss clearing, deliberate owner transfer, and automatic post-prune disappearance repair are exposed without SDL or pointer-ID knowledge. Pure tests cover slider-style outside drag bubbling/no-click-through, scrollbar release outside, transfer to a board-pan owner, cancellation, missing owners, and disappearing owners.
- [x] **UIE-07 — Define event consumption across nested controls.** Preserve ancestor hover bubbling, but let a child action such as build-cancel consume activation so the containing row does not also start a build. The API must remain immediate-mode and typed; do not add a callback bus.
  - COMMENT: Added typed immediate-mode `consumeFlag(source_key, comptime flag)` plus host `El.consume`. A successful call atomically observes and clears only that flag on the source and its stamped ancestor chain without allocation, preserving hover, unrelated flags, siblings, topmost/captured routing, and capture ownership. Descendants consume before ancestor action decisions, which re-query rather than retaining stale interaction copies. The nested capital-row cancel control now consumes `.clicked`; row hover still bubbles while build activation is suppressed without a manual boolean guard. Pure tests cover three-level ancestry, single consumption, flag specificity, sibling isolation, missing keys, and captured routing.
- [x] **UIE-08 — Expose enough local/global geometry conversion for overlays and board math.** Provide tested helpers for point/rect conversion relative to a node and its effective clip. Keep prior-frame reads explicit. Tooltips, pointer-centered zoom, popup placement, and drag thresholds must not duplicate ad-hoc coordinate subtraction.
  - COMMENT: Added generic `Point`, translation-only global/local point and rect helpers, and `Geometry` carrying a node's last stamped full global rect plus inherited global clip. `effectiveClipGlobal` intersects the node box with its inherited clip and `effectiveClipLocal` translates that result into node-local pixels. Explicit non-allocating `Ctx.priorGeometryOf`, `Node.priorGeometry`, and host `El.prior_geometry` return null for missing/unstamped slots; legacy rect reads delegate for compatibility. Pure and integrated tests cover exact round trips, negative origins, clipped/unclipped effective regions, missing and unstamped geometry, clip propagation, and compatibility; the shape predicate test now uses the shared point conversion instead of ad-hoc subtraction.
- [x] **UIE-09 — Keep stamping complexity in perspective.** Do not block the 61-hex board on an O(interactive) `stamp_rects` rewrite. Profile the complete five-pass frame first; optimize stamping only if measured frame data identifies it as material.
  - COMMENT: Added exact five-pass instrumentation for intrinsic sizing, relative/percentage/grow resolution, placement, stamping, and drawing. The normal pure solver remains clock/callback-free; an explicit diagnostic variant returns its three timings, while the host times aggregate stamp/draw loops across roots. A generic tested `FrameProfiler` emits 600-frame average/max microseconds and stamping share with clean report-window reset. A bounded 600-frame development smoke capture on the current Act I screen measured 4.7/6.9/7.8/8.2/2370.5µs averages respectively, with stamping at 0.3% of the measured total (93.6µs max). No stamping rewrite was made: this smoke evidence does not identify it as material, and the walk still must produce paint order. QA-17 remains the gate for controlled 20-action/50-recipe/dialog/full-board captures on the reference machine.

## 4. Host input, focus, and semantics — `src/ui_client/`, `src/res.zig`, `src/main.zig`

- [x] **INPUT-01 — Replace edge-only mouse input with a frame event/state model.** Track pointer position/delta, button pressed/held/released, wheel x/y, click count, active pointer ID/type, modifier keys, key press/repeat/release, text input, window focus, and cancellation. Reset frame edges in one place.
  - COMMENT: Added allocator-free `ui_client/input.zig` and made `Resources.input` its host-owned frame model. A single `beginFrame` clears all pointer/button edges, accumulated delta/wheel, click counts, key events, copied text, focus/cancel edges, and overflow reports while preserving position/identity, held input, modifiers, and focus. Main normalizes SDL mouse/touch/pen IDs, five mouse buttons, normalized touch coordinates, key press/repeat/release plus modifier snapshots, copied UTF-8 text, focus transitions, and cancellation; focus loss/cancellation synthesizes held releases and clears engine pointer capture. Existing hover, click, scroll, text-entry, Backspace, and Escape behavior now reads the model. Fixed key-event/held-key/text capacities expose overflow flags. Deterministic tests cover reset persistence, accumulation, click counts, held states, modifiers, text copying/overflow, focus loss, cancellation, and synthetic releases.
- [x] **INPUT-02 — Activate controls on release, not raw mouse-down.** A click begins on press, activates only when a valid release completes, and is suppressed after a drag/cancel. Keep an explicit press event for controls that need it. Verify build, action, offer, tab, disclosure, and close controls cannot double-fire.
  - COMMENT: Added host-owned `PointerActivation`: primary down routes transient `.pressed` and records the topmost stable key, pointer kind/ID, and origin; only the same pointer releasing over the same geometric target within a 4px threshold routes one `.clicked`. Drag, release elsewhere, target change/disappearance, pointer mismatch, focus loss, touch cancellation, and duplicate release suppress activation. Generic core gained non-semantic `targetAt`, `markTarget`, and `markKey` primitives while remaining unaware of click policy. Main routes mouse/pen/touch edges in SDL event order and ignores touch-synthesized mouse activation duplicates. Existing build/action/tab/ration/nested-cancel/Continue/restart controls inherit the shared release event without call-site double firing; text input uses explicit `.pressed` for inside/outside focus behavior. Pure and integrated tests cover immediate press without activation, matching release, all suppression paths, bubbling, capture versus geometric targeting, and one-shot build/action/offer/tab/disclosure/close category completions.
- [x] **INPUT-03 — Route input through the engine's ordered hit/capture mechanisms.** Pointer hover/press/release/wheel targets the topmost eligible slot. Modal scrims and popups block covered controls by construction; callers do not sprinkle `if (!modal_open)` guards across unrelated screens.
  - COMMENT: Added transient host `.released` and `.wheel` events alongside hover/press. Main now routes primary up through capture-aware `markTarget` before geometric click validation and routes every SDL wheel event at its supplied pointer position; both scroll implementations require their viewport's routed `.wheel` before applying the frame delta. All four pointer event types therefore share the engine's reverse paint-order, clip/shape/pass-through, ancestor bubbling, and capture-first mechanisms. Opaque tooltip popup roots and fullscreen modal scrims now query interaction slots; because independent roots stamp in draw order, later overlays structurally occlude covered controls without caller guards, while explicit `pass_through` remains the opt-out. No production modal-open guards or overlay call sites existed to migrate; obsolete guard guidance was removed. Pure tests cover scrim/dialog and popup-over-control routing for hover, press, release, wheel, and click, including interior bubbling, outside targeting, and ordinary targeting beyond popup bounds.
- [x] **INPUT-04 — Add interaction states required by the visual contract.** The host vocabulary must represent at least hover, pressed/held, released/clicked, disabled, focused/focus-visible, selected/checked, and dragging/captured where applicable. Keep semantic state (selected/disabled) authoritative in widget/domain state rather than as unowned latching flags.
  - COMMENT: Replaced the unused unowned `active` latch with the complete host vocabulary: transient hover/pressed/held/released/wheel/clicked/dragging/captured plus persistent disabled/focused/focus-visible/selected/checked projections. Main republishes held on the stable press key, dragging after the 4px threshold (including release-position movement), and captured only on the exact capture owner; cancellation and the normal transient reset clear them. Added `ControlState`/`publishControlState`, which writes every semantic field including false each build. Authority remains in caller enabled/affordability/readiness and build classification, the focus registry, pooled `TabsState`/`BuildViewState`, and simulation `Metabolism.setting`; no interaction flag self-toggles. Stock/game buttons, action tiles, build rows/cancel/goal controls, tabs, ration choices, build sort/filter choices and the built checkbox now publish states and use held chrome. Text input publishes focus/focus-visible from engine focus (current policy visibly outlines all focus). Tests cover the full vocabulary, transient reset, semantic persistence plus explicit false republishing, activation ownership accessors, and held/dragging/exact-capture publication. Concrete capture/drag controls remain INPUT-05.
- [x] **INPUT-05 — Wire pointer capture and drag thresholds.** Sliders, scroll thumbs, and board panning use capture. Board pan starts only after a 4px logical movement threshold and suppresses the tile click generated by the same gesture. Cursor state supports default, pointer, text, grab/grabbing, horizontal-resize, and not-allowed where SDL/platform APIs permit.
  - COMMENT: Both production scroll implementations now capture the exact thumb on primary press, preserve the original down position across same-batch motion, drag beyond the narrow track through capture-first routing, map travel to a clamped offset, apply the final release position, and owner-release; cancellation, lost ownership, disappearing overflow, and generic prune repair stop safely, while unrelated pointer identities cannot move the owner. Added reusable `ThresholdDrag`, sharing `PointerActivation`'s strict `>4` logical-pixel boundary: it emits no pan delta through 4px, while crossed gestures use the existing activation state to suppress their paired click. No production slider or Act II board exists at this roadmap stage, so this establishes their tested capture/threshold policy without inventing game content. Added frame-reset cursor requests for default/pointer/text/grab/grabbing/horizontal-resize/not-allowed; current controls publish pointer/text/not-allowed and scroll thumbs publish grab/grabbing. The SDL host owns cached system cursors, falls back to default when unavailable, and maps both grab variants to SDL's `move` shape because SDL 3 exposes no distinct grab cursors. Focused tests cover exact threshold crossing/deltas, scroll travel/clamping, same-batch origin, outside capture, release-position application, cleanup, request reset/ordering, and every SDL mapping.
- [x] **INPUT-06 — Implement keyboard command routing.** Support Tab/Shift+Tab, arrows, Enter, Space, Escape, `/`, Backspace/Delete, Home/End, and text input without the current global Escape-to-quit conflict. The focused overlay/view receives Escape first; quitting is a separate unhandled command.
  - COMMENT: Added host `Command` mapping for Tab/Shift+Tab, four arrows, Enter/Space, Escape, `/`, Backspace/Delete, and Home/End with explicit one-shot versus repeat policy, plus a fixed double-buffered registry for prior-build text/search owners and topmost Escape targets. Main routes global focus traversal, roving arrow movement with unified `.clicked` activation, Enter/Space activation, slash search focus with slash-byte suppression, registered-owner editing commands/text, and transient `.dismissed`. A focused text owner consumes Escape first; otherwise the latest overlay/view target receives it; unhandled Escape never quits, while SDL quit/terminating remains the separate exit path. Stock/game buttons, icon buttons, actions, build rows/cancel/goals/chips, tabs, and ration choices now register enabled focus targets, pointer activation requests focus, exclusive choices use roving groups, and every focused control publishes visible focus chrome. Modal and text-input primitives register Escape and editing/search ownership; no production modal/search/board exists yet. Delete/Home/End route to the current end-anchored editor, with caret/selection behavior deliberately remaining INPUT-07. Tests cover every mapping/repeat rule, registry publication/pruning/topmost precedence, focused-roving movement, dismissal reset, focus lifecycle, and existing input/text behavior.
- [x] **INPUT-07 — Complete single-line editing.** Search fields support UTF-8 insertion/deletion, caret movement, selection, Home/End, clipboard shortcuts when available, placeholder, clear action, focus-visible chrome, and an explicit maximum query length with visible/non-silent refusal.
  - COMMENT: Replaced the primitive 64-byte end-only `TextInputState` with an authoritative host editor model, `ui_client/editor.zig`'s `LineEditor` (aliased as `TextInputState` so the pool still auto-registers). It holds a fixed `max_query_bytes = 128` UTF-8 buffer plus byte-offset caret/anchor that always sit on codepoint boundaries: codepoint-wise insertion/deletion, Backspace/Delete that delete the selection when present, plain-vs-`extend` caret movement (Left/Right/Home/End), anchored selection, select-all, clear, and cut/copy accessors. Every insertion/paste is validated as admissible single-line UTF-8 (no C0/C1/DEL, no line breaks) and against the max length *after* replacing the selection, and is refused **as a whole** — never truncated or stripped — raising a `refused` latch that the widget surfaces as a `danger` outline until the next accepted edit. `command.zig` gained `select_left/right/line_start/line_end`, `select_all`, `clipboard_copy/cut/paste`, and `clear_field`, mapping Shift+Left/Right/Home/End and one-shot Ctrl+A/C/X/V (Alt cancels the accelerator so plain typing is untouched). `main.zig` only routes: it feeds SDL text through `insert`, drives caret/selection/delete commands into the focused registered owner's model, and bridges the SDL 0.15.2 clipboard (`hasText`/`getText`+`free`/`setText`) for copy/cut/paste, with paste passing through the model's whole-text validation. The generic `src/ui` stays unaware of editor semantics; `ui_client` owns model and policy. The widget renders placeholder (dim, unfocused-empty), a caret bar or guillemet-bracketed selection from the model, focus-visible/refusal chrome, and a pointer "✕" clear affordance (keyboard clear is Ctrl+A then Backspace, same model). No production search consumer exists yet, so the stock widget/model is tested honestly rather than against invented game content. Added deterministic, SDL-free exhaustive tests to `test-ui`: 20 editor tests (UTF-8 boundary movement, selection replace, Backspace/Delete selection semantics, whole-buffer overflow/invalid/multi-line refusal and its non-silent flag, net-length paste-over-selection, clear, cut) plus new `command.zig` chord tests for the Shift/Ctrl mappings and repeat/Alt policy. `zig fmt`/`ast-check`, `zig build test-ui`, `zig build test`, and `zig build` all pass; LSP reports no diagnostics. General `TextState`/TEXT-01 is unchanged.
- [x] **INPUT-08 — Add semantic metadata to interactive nodes/templates.** Represent role, accessible label, value text, selected/expanded/disabled/checked state, relationships where useful, and polite live announcements. Keep it host-side if core need not inspect it.
  - COMMENT: Added `ui_client/semantics.zig`, a fully host-side accessibility model that is the source INPUT-09's platform bridge will read (not a second source of truth). A `SemanticNode` carries the stable `node.key`, a `Role` (button/icon_button/radio/checkbox/text_input/progress_bar/tile/group/dialog/text), an **owned** accessible label and an optional **owned** value text in fixed `OwnedText` buffers, `SemanticState` (disabled/focused/selected/checked/expanded), stable-key `Relations` (`controls`/`described_by`, bounded to `max_relations` with explicit overflow refusal), and a `LiveRegion` policy. `SemanticRegistry` is a fixed, double-buffered table (128-node cap) built in UI/control paint order and published after build/end-frame exactly like `command.Registry`: `beginBuild`/`publish`/`endBuild` swap buffers, `snapshot` exposes the prior completed build, and because every string is copied into owned node storage the snapshot never borrows the frame arena. Duplicate keys update in place preserving first-seen order (deterministic); node overflow past capacity and per-field length refusal are surfaced non-silently via `snapshotOverflow`/`snapshotFieldRefused` and `OwnedText.truncated` (a too-long field is refused whole, never a cut prefix — the editor's policy). `AnnouncementChannel` is a bounded polite live-region queue (16 cap) with owned text, a monotonic session `generation`, consecutive-duplicate dedup, explicit `overflow` refusal for a full queue or over-long message, and a `drainThrough` cursor; it is presentation plumbing only and holds no simulation authority. Lifecycle is wired in `Resources` (`semantics`, `announcements`) and `main` (`beginBuild` before `build_ui`, `endBuild` after `endFrame`). Representative semantics are published from the *authoritative* widget/domain facts right where each control already calls `publishControlState`, so state can never disagree with the interaction pool/focus owners: stock `button`/`icon_button`/`progress_bar`/`text_input`/`modal`, and game `tabs`/`ration_dial` (radios linked to a named group), `build_list` sort/show/tier radio groups + the built checkbox + the goal action button, `capital_row` build rows (a tile linking its cancel sub-action), and `action_tile` (tile with a running-progress value readout). Honest limitations recorded rather than invented: the stock icon button and progress bar have no widget string for a name, so they publish an empty label (a named variant/domain helper supplies one); `expanded` has no interaction-pool equivalent and is owned only by the dialog shell; and no production board/search/modal *consumer* exists yet, so the model describes the controls that do exist. `src/ui` stays generic and accessibility-unaware. Added deterministic SDL-free tests to `test-ui` covering owned-text copy/whole-refusal, prior-snapshot ownership across the frame swap, paint-order + duplicate-key in-place update, node-overflow and field-truncation refusal, state/relationship mirroring and relation overflow, text-input value/refusal live region, progress-bar readout, and announcement dedup/generations/overflow/drain. `zig fmt`, `ast-check`, `zig build test-ui` (129 tests), `zig build test`, and `zig build` all pass; LSP reports no diagnostics. Platform exposure of this tree to Windows accessibility APIs remains INPUT-09.
- [x] **INPUT-09 — Implement and document the platform accessibility bridge.** Expose the semantic tree to Windows accessibility APIs if feasible through the chosen SDL/native integration. At minimum, ship full keyboard operation, visible focus, non-color state cues, and live-announcement plumbing; explicitly record any screen-reader limitation rather than claiming HTML ARIA parity that SDL does not provide.
  - COMMENT: Added `ui_client/a11y.zig`, a fully host-side accessibility **bridge** that consumes the INPUT-08 model rather than re-deriving it: a `Bridge` with a deterministic, allocation-free lifecycle mirroring `command.Registry`/`SemanticRegistry` — `init(provider)` → per-frame `poll(snapshot, overflow, field_refused, channel)` → `deinit()` — plus a `Status` (`active`/`inactive`) and an honest `Capabilities` report. `poll` hands the just-published `semantics.snapshot()` to the provider as its tree root, folds the registry's non-silent `snapshotOverflow`/`snapshotFieldRefused` flags into `last_snapshot_overflow`/`last_snapshot_field_refused`, then forwards every announcement newer than a monotonic `spoken_generation` cursor to the provider and calls `AnnouncementChannel.drainThrough` so the bounded queue never stays full. The bridge borrows the snapshot only for the call and copies the last message into an owned `OwnedText(value_cap)` — it holds no frame-arena or channel-owned pointer across frames. The future Windows UIA seam is a narrow `Provider` vtable (`getRoot`/`elementFromKey`/`mapRole`/`raiseFocus`/`raiseAnnouncement`) — exactly what a `WM_GETOBJECT`-answering `IRawElementProviderFragmentRoot` needs — with an inert `NoopProvider` default (no element, custom role id `0`, no-op raises), so a real provider slots in without touching the bridge, `src/ui`, or the semantics source. Wired in `Resources` (`a11y`, defaulting to the noop provider) and driven in `main` right after `semantics.endBuild()`. **Shipped/verified roadmap minimum:** full keyboard operation (`command.zig` + focus routing, INPUT-06/07), visible focus chrome (INPUT-04/06), non-color state cues (outline/shape `ControlState` projections, never color alone, INPUT-04), and live-announcement plumbing (this bridge drains `AnnouncementChannel` to the provider sink). **Honest limitation, not a fake claim:** this build exports **no** live screen-reader tree, and `Capabilities.screen_reader_export` is `false` (a test asserts this so it cannot silently flip). Cause: a UIA provider must answer `WM_GETOBJECT`, which requires hooking the SDL-owned window's `WndProc`, but `zig-sdl3` 0.1.6 does not implement `SDL_SetWindowsMessageHook` (commented out in the binding's `system.zig`; only the X11 event hook exists). The only alternatives — hand-rolled `WndProc` subclassing on the native `HWND` or forking the vendored SDL binding — are the risky, policy-leaking dependencies this slice forbids, and SDL3 offers no accessibility abstraction on Windows to build on; so genuine HTML/ARIA parity is unavailable and is not claimed. All Windows/platform policy stays in `ui_client`; `src/ui` remains generic and accessibility-unaware (grep-clean). Added deterministic SDL-free tests to `test-ui` covering capability honesty (screen-reader export asserted false), the noop provider's inert/honest behavior, snapshot forwarding as the tree root, generation-cursor advance + `drainThrough`, no-re-speak across a provider swap, snapshot overflow/field-refusal surfaced through the bridge, an inactive guarded-no-op poll, and the provider seam's role mapping/element lookup/focus raise via a recording fake. `zig fmt`, `zig ast-check`, `zig build test-ui` (137 tests), `zig build test`, and `zig build` all pass; LSP reports no diagnostics. A live Windows UIA provider against the seam remains future work (blocked on an upstream message-hook binding).
- [x] **INPUT-10 — Add reduced-motion policy.** Read a platform preference when possible and expose `view.reduced_motion`; all optional transitions snap when enabled. Functional state changes and focus remain visible without motion.
  - COMMENT: Added `ui_client/motion.zig`, a fully host-side reduced-motion **policy**. A one-bit `Policy { reduced_motion }` is resolved **once at host init** in `App.setup` (right after `Resources.init`) via `resolveFromEnv(allocator, PlatformProbe.instance())`, stored on `Resources.motion`, and then only *projected* onto `View.reduced_motion` in `build_ui`'s prologue each frame (next to the theme install) — no per-frame OS call. **Resolution precedence** is total and non-panicking: an explicit override (env `HA_REDUCED_MOTION`, or an injected config bool) wins; else the platform probe; else a deterministic `false` (motion allowed) fallback. The override parser `parseOverride` is case-insensitive and total — `1/true/yes/on`→true, `0/false/no/off`→false, and **anything else including empty/whitespace/garbage is ignored** (`null`, falls through to the probe), which the tests pin so bad input can never panic. **Platform query, honest about its limits:** on Windows the probe calls `SystemParametersInfoW(SPI_GETCLIENTAREAANIMATION, …)` **once** — the documented live-state "client area animation" flag behind Settings → Accessibility → Visual effects → Animation effects (`reduced_motion = !animations_enabled`); it links against `user32`, already linked by the vendored SDL C build, so no `build.zig` change and no registry read (`MinAnimate` does not reflect temporary state). SDL exposes no cross-platform reduced-motion abstraction (`zig-sdl3` 0.1.6 has only mouse hints + a light/dark theme query), so on every non-Windows target the probe returns a deterministic `null`→`false` rather than pretending a preference was read. The probe is a narrow injectable `Probe` vtable (mirroring INPUT-09's `a11y.Provider`), with `PlatformProbe` (real query), `FixedProbe`, and `nullProbe` seams so every test stays SDL/OS-free. **Motion inventory + functional/decorative split (grepped all of `src` for lerp/tween/animat/transition/pulse/interpolate/ease/sin/cos/phase/progress/clock):** the action-tile running underbar (`1 - remaining/total`) and the ration-dial fill (fraction through the current food unit) are *functional determinate progress/simulation readouts* — they MUST and DO remain visible under reduced motion, unchanged; the day counter is an integer readout; `std.time.Timer` uses are the profiler; `elapsed`/`dt` elsewhere are sim ticks/RNG, non-visual. The **only** genuinely decorative clock-driven oscillation is `status.heartbeat_color` (a sine-blended color), used solely on the dev `mock.zig` showcase (not on any production screen — `play_game`/`gameover`/`act_one_end` never call it). It is the one concrete migration: it now takes the policy and, under reduced motion, freezes its sine phase to the `0.5` midpoint (via `Policy.phase`) instead of oscillating. There were therefore **no production optional transitions to migrate** (no tween/keyframe engine exists yet — that is RENDER-08). To serve that future work the policy ships a reusable **snap gate** — `Policy.snap(T, to, value)` returns the end state `to` when reduced motion is on, else the caller's interpolated `value` — so every future optional transition routes through one policy check while functional progress/state/pointer/focus cues and the simulation are never gated. Wired in `Resources` (`motion`, default motion-allowed), `main` (`resolveMotionFromEnv` once at setup), `res.View` (`reduced_motion`), and `pages/root.build_ui` (project per frame); re-exported flat from `ui_client/root.zig` (`MotionPolicy`, `MotionProbe`, `PlatformMotionProbe`, `resolveMotionFromEnv`, `parseMotionOverride`, …). `src/ui` stays generic and motion-unaware. Added deterministic SDL-free tests to `test-ui` (13 new; 150 total): `parseOverride` true/false truth tables + case-insensitivity, empty/whitespace/garbage→null (no panic) and whitespace trimming, `resolve` precedence (override beats probe beats `false` fallback), probe-only-when-no-override, the `false` fallback, `nullProbe`, the non-Windows deterministic `null`, `resolveFromEnv` falling back to the probe, and the `snap`/`phase` gates for both branches and multiple channel types. `zig fmt`, `zig ast-check`, `zig build test-ui` (150 tests), `zig build test`, and `zig build` all pass; LSP reports no diagnostics. A live Windows UIA/animation-event subscription and the actual tween engine that consumes the `snap` gate remain RENDER-08.

## 5. Text and typography — `src/font.zig`, `src/ui_client/features/text.zig`, style

- [x] **TEXT-01 — Remove silent 64-byte truncation.** Replace fixed `TextState.buf` behavior with owned/capacity-managed cached text or a safely larger explicit cap that reports overflow. Pool eviction must free owned memory correctly. Apply the same rule to text input and log/display strings.
  - COMMENT: The persistent, pooled rendered-text cache `UiState.TextState` in `ui_client/ctx_binding.zig` was previously **untouched** by INPUT-07 (its COMMENT explicitly noted "General `TextState`/TEXT-01 is unchanged") and still silently `@min`-truncated any string past its fixed 64 bytes — cutting a long line's tail and potentially severing a UTF-8 codepoint. It is now migrated to the codebase's proven **copy-or-refuse-whole** contract, the same non-silent policy `semantics.OwnedText` and the INPUT-07 editor already use. `TextState` gains an explicit `cap = 256` (comfortably holds real UI strings — labels, values, a full 128-byte search query rendered as text, the game's short status/log copy — with headroom) and a `refused` flag; `update` now copies a source in **full** or **refuses it as a whole** (clearing `len`, latching `refused`, returning acceptance) rather than storing a cut prefix, and `text()` returns `null` for a refused/empty state so the renderer draws **nothing** instead of a severed string. Chose this fixed-cap **Option A** over an allocator-backed growable buffer deliberately: it keeps `TextState` **POD** — no allocator, no `deinit` — so the cache `Pool` contract (init on fresh *and* reused slots, no eviction resource, handles-not-pointers growth safety) is entirely **unchanged**, there are **no new allocator lifetimes** on the per-frame text path, and "free owned memory exactly once" is moot because the buffer is inline and owned by value (pool movement/reuse/teardown can never double-free or dangle it). TEXT-02 wrapping/ellipsis remains the mechanism for text that must genuinely exceed the cap. Fixed the pre-existing **measure-vs-draw mismatch** in `features/text.zig attach`: it now measures the string the state actually accepted (`st.text() orelse ""`) instead of the caller's raw argument, so a refused/over-cap string reserves a **zero content box** rather than a correctly-sized box that then renders a cut string — box and render agree, and `style.apply`'s re-measure already reads the same `st.text()` accessor so heading re-measurement stays correct. Applied the identical rule to the **log feed** (`log.zig`): `Entry` grows to `max_len = 256` with a `refused` flag, and `Log.push` copies a message in full or, past the cap, stores an explicit visible `overflow_marker = "(log message too long)"` and sets `Entry.refused` — never a silently cut / mid-codepoint prefix. `push` intentionally stays `void` (the refusal is observable on the stored `Entry`, not a return value) so the many fire-and-forget sim/UI call sites in `systems.zig`/`actions.zig`/`capital.zig`/`main.zig`/`pages` are unchanged. Exhaustively inspected the other fixed buffers and `bufPrint ... catch` paths and left the out-of-scope ones alone: the `pages/`/`fmt.zig`/`widgets.zig` numeric/label formatting buffers **fail visibly** (`catch "?"`/fallback substitute), `input.appendText` already sets a non-silent `text_overflow` flag, `main.zig`'s clipboard temp is bounded to the editor's own cap by construction, and `features/line.zig`'s `LineState` is documented variable-length point data, not rendered text — none is a silent persistent-string cut. INPUT-07's `LineEditor` (`max_query_bytes = 128`, whole-edit refusal, visible `refused` latch) is preserved verbatim: text input keeps its explicit visible 128-byte refusal rather than becoming unbounded, guarded by a new regression test. The generic engine (`src/ui`) and the simulation/UI boundary are untouched; all TEXT-01 policy stays host-side in `ui_client`/`log.zig`. Added deterministic SDL-free tests: `TextState` full-copy round-trip, exactly-at-cap acceptance, over-cap whole-refusal (len 0, `refused` set, `text()` null — never a prefix), long multibyte-UTF-8 whole-refusal (proving no mid-codepoint cut) plus intact storage of a fitting multibyte string; `TextState`-typed `Pool` integration under the deterministic testing allocator — fresh/reused-slot clean init via the pool contract, value survival across pool growth (handle not pointer), and clean teardown under a `FailingAllocator` (the POD no-leak guard); log full-copy, at-cap, over-length marker refusal, long-UTF-8 marker refusal, ring-overwrite/newest-first correctness across a refusal, and `clear`; direct `features/text.zig` measure-vs-render agreement coverage (shared `State.text()` source, zero box + no draw on refusal); and the INPUT-07 128-byte whole-edit refusal regression guard. `zig fmt`, `zig ast-check`, `zig build test-ui` (166 tests), `zig build test` (195 tests), and `zig build` all pass; LSP (zls 0.15.1) reports no diagnostics on the changed files; `git diff` reviewed. Rendered-text texture caching/invalidation (TEXT-05) and multiline wrapping/ellipsis (TEXT-02/03) remain their own slices.
- [x] **TEXT-02 — Add constrained multiline text.** A text block accepts a maximum width, wraps on word boundaries with a deterministic hard-break fallback, reports measured width/height/baseline, and renders exactly the measured lines. Preserve a fast single-line path for dense rows.
  - Keep the engine callback-free by measuring in the host with an explicit constraint derived from `ViewMetrics`/template layout. If later layout-dependent measurement is unavoidable, add a narrow documented measure phase rather than hidden relayout.
  - COMMENT: Added constrained multiline entirely host-side, leaving the generic engine (`src/ui`) callback-free and typography-unaware. The core is a new **pure** line-breaking routine `ui_client/features/wrap.zig`: `wrapLines(text, max_w, measure, ...)` walks the bytes once, yielding each line's byte-span through an inline `emit` callback, with **no allocation and no I/O**. It is deliberately parameterized by a tiny `wrap.Measurer` interface (`width(bytes)` + `prefixBytes(bytes, max_w)`) rather than reaching into SDL, which is what makes it both callback-free at the engine boundary *and* testable without a live font. The algorithm is greedy word wrap over an explicit pixel width: `max_w <= 0` is the **single-line fast path** (one line per newline segment — the common no-newline case is one line, byte-for-byte the prior dense-row behavior, so dense rows pay nothing); a positive width accumulates space-separated words while the candidate line fits, forces a break on `\n` (and yields a genuine empty line for `\n\n`), collapses leading/interior/trailing run-of-space at every break, and — when a **single word alone** exceeds the width — hard-breaks it on **codepoint boundaries** via `TTF_MeasureString` (`prefixBytes`), always advancing at least one whole codepoint so tiny positive widths terminate and never split a UTF-8 sequence. **Chose host-driven breaks over SDL's own `TTF_GetStringSizeWrapped`/`renderTextSolidWrapped` deliberately** (documented in `wrap.zig`): SDL owns the break points, does not guarantee its measured line count equals a separate render byte-for-byte, and its over-long-word handling is not the deterministic UTF-8-safe hard-break this slice requires — driving the breaks here is what lets "renders exactly the measured lines" be a **guarantee**. `features/text.zig` backs the `Measurer` with the live `sdl.ttf.Font` (`FontMeasurer`), and both `attach` (build-time measure) and `draw` (per-line render) run the **same** `wrapLines` over the same metrics, so the reserved box and the drawn glyphs cannot drift — the TEXT-01 "measure and draw read one source" invariant, extended to N lines. Measurement (`measureWrapped` → shared `text.remeasure`) reports `data_width` = widest line, `data_height` = `line_count * font.getLineSkip()` (line skip chosen over tight per-glyph height for consistent stacked leading), and `baseline` = the **last line's** descent (`lineSkip - ascent`) so a wrapped block still baseline-aligns as the final line in a `.row`. No new `SizeRule` was needed: `.content` already sizes to `data_*`, and layout stays pure (core reads `data_*`, never measures). No hidden relayout — a single build-time measure, re-measured only on the documented `font`/`wrap` change, exactly like today's heading re-measure; `style.apply`'s `font` re-measure now routes through the same `text.remeasure` so a heading (or wrapped block) re-wraps at the same constraint. The API is minimal and keeps wrap as **placement, not style**: `TextState` gains a POD `wrap_width: f32 = 0` (0 = fast path; line spans are recomputed by the pure routine, never stored, so the pool contract stays allocator-free/POD and TEXT-01's `cap = 256` **whole-refusal** is untouched — a too-long source is still refused whole and reserves a zero box, wrap or not), `El.with_wrap(max_w)` sets the width and re-measures imperatively (like `with_size`), and `el.textWrapped(...)` is one-call sugar; single-line `el.text` is unchanged. **INPUT-07**'s `LineEditor` (`max_query_bytes = 128`, whole-edit refusal) is entirely separate and untouched. Integrated at two representative prose sites with **real explicit template widths** (no `ViewMetrics` yet — VIEW-01 is unchecked, and the explicit width parameter *is* the documented seam it will later feed): `pages/templates/log_view.zig` wraps each 256-cap log entry to the scroll viewport's fixed content column (`width - scrollbar_w`) instead of clipping, and `pages/act_one_end.zig` wraps the Act I curtain **note** prose to an explicit dialog column (`dialog_prose_w = 320`) so it reads as a paragraph rather than stretching the `fit_children` panel to one long line. No TEXT-03 clipping/ellipsis and no TEXT-04 typography changes were done. Added comprehensive **SDL-free** deterministic tests in `wrap.zig` behind a 1px/byte fake measurer (so pixel width == byte count and every case is exact): single-line fast path identical to prior behavior, fast-path newline splitting, ASCII greedy wrap, greedy multi-word packing, **exact-fit stays / one-pixel-less breaks**, over-long single word hard-break, UTF-8 multibyte word never split mid-codepoint, leading/trailing/multiple space collapse, all-space paragraph → one blank row, embedded `\n`, `\n\n` blank line, empty string → zero lines, **tiny positive (1px) width** terminates emitting one codepoint per line, `emit`-stop line budget, and **measure/render agreement** (same inputs → byte-identical spans); plus `features/text.zig` state tests for `wrap_width` POD round-trip, default fast path, and over-cap whole-refusal preserved with wrap set (all pulled into the feature test binary via `_ = wrap`). `zig fmt --check`, `zig ast-check` (all changed files), `zig build test-ui` (184 tests, up from 166), `zig build test` (213 tests, up from 195), and `zig build` all pass; zls reports no diagnostics on the changed files. Rendered-text texture caching (TEXT-05), overflow clip/ellipsis (TEXT-03), the typography contract (TEXT-04), and `ViewMetrics` (VIEW-01) remain their own slices.
- [x] **TEXT-03 — Add single-line overflow modes.** Support clip and ellipsis for stock tokens, catalog cells, status copy, and narrow layouts. Hit/box geometry remains the allocated cell, not the unbounded glyph width.
  - COMMENT: Added two **explicit** single-line overflow disciplines for a text *cell* of an allocated width — `.clip` and `.ellipsis` — entirely host-side in `ui_client`, with **no generic engine change** (the only engine primitive used is the pre-existing `Layout.overflow = .clip`, applied for a clip cell's *subtree*). The vocabulary is explicit, never implicit: `TextState` (`ui_client/ctx_binding.zig`) gains a POD `Overflow = enum { visible, clip, ellipsis }` plus `overflow: Overflow = .visible` and `overflow_width: f32 = 0` (a **required nonnegative** allocated cell width; `0` is a legal zero-width cell). `.visible` + 0 is byte-for-byte the prior fast/wrapped path, so dense rows and ordinary labels pay nothing. Overflow and `wrap_width` are **mutually exclusive** (a cell is single-line, wrapping is multiline); `El.with_cell(mode, cell_w)` asserts `cell_w >= 0`, clears `wrap_width`, and re-measures imperatively like `with_wrap` — placement, not style. **The core geometry rule:** in `text.remeasure`, an overflow cell writes `node.size.data_width = overflow_width` — the layout box *and* the hit box (which reads the stamped allocated rect) are the allocated cell, **never** the unbounded glyph width, so a widening label can never shove its neighbors and a shorter one still reserves its column; `data_height`/`baseline` still come from the font (single line), so a cell baseline-aligns in a row exactly like a plain label. **Clip mode** (`text.draw` → `drawClipped`) blits the whole accepted string but scopes the SDL renderer clip to the intersection of the *prior* clip (snapshotted with both `renderer.getClipEnabled` and `renderer.getClipRect`, because the binding maps an enabled zero-area clip to `null` just like disabled clipping) and the content cell, then **restores the normalized prior state in a `defer`, including on any error path** — preserving the enable bit is essential because a fully clipped ancestor must remain fully clipped, and a text leaf can sit inside an ancestor `.clip` (a scroll viewport) where dropping that clip would leak the string outside the ancestor's box. This is necessary because the engine's `Layout.overflow = .clip` crops a node's *children*, but a text leaf paints its own glyphs before that narrowing — so the crop is applied here, renderer-scoped and reverted; the cell additionally sets `Layout.overflow = .clip` so any decoration subtree and hit-testing get the same box for free. **Ellipsis mode** (`text.draw` → `drawEllipsized`) reuses a new **pure, SDL-free** helper `wrap.ellipsisFit(text, cell_w, ellipsis_w, measure)` (deterministic, one source of truth for measure and render, testable with the same 1px/byte fake as `wrapLines`): if the whole string fits `cell_w` it draws it whole (no ellipsis); otherwise the glyph budget is `cell_w - ellipsis_w`, and if that budget `<= 0` (cell narrower than the ellipsis token itself) it deterministically draws **nothing** rather than a floating ellipsis wider than the cell (no overdraw, ever), else it selects the longest **codepoint-aligned** prefix via `measure.prefixBytes` (UTF-8-safe, never mid-sequence) and draws that prefix plus the deterministic ellipsis token `"\u{2026}"` (U+2026), whose width is **font-measured, not assumed**; if a live SDL font measurement fails, `drawEllipsized` returns without drawing the cell. `draw` uses that one fit helper consistently, while `remeasure` deliberately reserves the explicit allocated cell width independent of glyph fit, so the box is always the allocated cell and only the drawn glyphs change — the TEXT-02 "one routine, both callers" invariant extended to the single-line cell. The single-line/wrapped/overflow blits were consolidated onto one `drawSpan` (the former `drawLine` folded in) so every path rasterizes and skips a missing-glyph frame identically. **TEXT-01** (`cap = 256` whole-refusal, `text()` → null) is untouched — a refused string draws nothing and reserves the cell (or zero) box, never a cut string, with an overflow cell set. **TEXT-02** wrapping is unchanged and takes precedence if both are set. **INPUT-07**'s `LineEditor` is a separate model, not touched. Integrated at two representative sites with real explicit template widths (the documented VIEW-01 seam, like `with_wrap` today): `pages/templates/capital_row.zig`'s catalog **name column** now uses a `.ellipsis` cell of exactly its `w_name = 118` width (a long good name elides instead of stretching the row and knocking every downstream column out of its lane; short names draw in full), and `pages/mock.zig`'s vitals **heartbeat status readout** uses a `.clip` cell of 64px (a narrow-layout status copy whose row geometry stays fixed regardless of the copy). Added **SDL-free** deterministic tests: in `wrap.zig` for `ellipsisFit` — exact fit → no elision, one-pixel-less → codepoint-aligned prefix + ellipsis fitting the cell, multibyte prefix never split (U+2603), budget-zero and cell-narrower-than-ellipsis → deterministic empty (no overdraw), zero-width cell, tiny sub-codepoint cell → empty prefix + ellipsis, font-error (width→0) degrades to no elision, and determinism (measure == draw); and in `features/text.zig` for the state — overflow mode/width POD round-trip and `.visible`+0 default fast path, over-cap whole-refusal preserved with an overflow cell, the allocated-cell width rule (`data_width` from the field, independent of glyph width — including a legal zero-width cell that remains zero-sized and draws nothing), mode switching back to `.visible` clearing the constraint, the ellipsis token being a single valid U+2026 codepoint, exact disabled-vs-enabled-empty clip-state normalization, and integer clip-rect intersection (overlap + disjoint→zero-area). `zig fmt`, `zig build test-ui` (200 tests, up from 184), `zig build test` (229 tests, up from 213), and `zig build` all pass; zls reports no diagnostics on the changed files. Rendered-text texture caching (TEXT-05) and the typography contract (TEXT-04) remain their own slices.
- [x] **TEXT-04 — Implement the prototype typography contract.** Load JetBrains Mono, disable ligature assumptions, support body `14`, small `11`, heading `21` logical px, regular weight, global `-0.025em` tracking, heading `-0.04em` tracking, and uppercase eyebrow tracking in the `0.05–0.09em` range. Centralize logical-to-device scaling and cache fonts/textures by all render-affecting attributes.
  - COMMENT: Implemented the prototype typography contract entirely host-side in `ui_client` (+ `res.View`), leaving the generic engine (`src/ui`) typography- and scale-unaware and preserving the TEXT-01..03 contracts byte-for-byte on their untracked fast paths. **Font asset + license/provenance:** the correct **`JetBrainsMonoNL-Regular.ttf`** ("No Ligatures" build) was already present, so it was kept unmodified; the OFL gap was closed — added `assets/fonts/JetBrainsMono-OFL.txt` (verbatim SIL OFL 1.1 + JetBrains 2020 copyright, from upstream `JetBrains/JetBrainsMono`) and `docs/fonts.md` (provenance: source URL, the `NL` = ligature-free rationale, Regular-400-only, and the Reserved-Font-Name note that we ship the *Original Version* under its original name so §3 is not triggered). `build.zig` now `installFile`s **both** the TTF and the OFL text into `zig-out/assets/fonts/`, so a packaged build is self-contained and satisfies OFL §2 (verified: both files present in `zig-out/assets/fonts/`). **Centralized roles + one logical→device seam:** new SDL-free `ui_client/type.zig` declares the contract as one `Role{ size_logical_px, tracking_em, transform }` table — body `{14,-0.025,none}`, small `{11,-0.025,none}`, heading `{21,-0.04,none}` (the prototype `--h3`), eyebrow `{11,+0.07,upper}` (mid of the 0.05–0.09em band) — all **regular weight**, with `default_logical_px` defined *from* `body` so unstyled==body. `toDevice(logical_px, scale)` is the **single** logical→device multiply (clamped ≥1px, non-positive scale treated as 1); `deviceTracking(em, device_px)` rounds `em*px` to an **integer** device pixel (crisp per RENDER-06, and the rounding is what makes measure and draw bit-identical). `View` gains one `scale: f32 = 1` field — the VIEW-01 seam — so today device px == logical px and nothing changes visually while every font size already routes through the seam (`text.attach` seeds the default size through it; `style.apply` resolves role sizes through it; `log_view` line-height too). **Style projection (RENDER-02 payload):** `Style` gains optional `tracking: ?f32` (em) and `transform: ?Transform`; the role presets (`style.body/small/heading/eyebrow`, `h3` retained as a `heading` alias, legacy `h1`/`h2` kept as above-contract display sizes inheriting global tracking) are the fragment projection of the `type` roles, composing last-non-null-wins for free through the existing field-wise `merge`. `style.apply` applies the whole typography triple together and re-measures **once** through the one shared `text.remeasure` routine: case transform first (ASCII-only, **length-preserving** in-place fold, so byte offsets used by wrap/clip/ellipsis stay valid and idempotent across re-apply), then size via `toDevice`, then tracking via `deviceTracking` at the final device px stored as POD `TextState.tracking`. **Tracking with measure==render agreement (the hard core):** SDL_ttf has no letter-spacing API, so tracking is host-side and flows through the ONE shared routine both passes use. `TextState` gains a POD `tracking: f32 = 0` (device px; `0` = the untracked fast path — one `getStringSize` measure and one `renderTextSolid` span, byte-for-byte the pre-TEXT-04 behavior). When non-zero, a new `walkClusters` iterates UTF-8 codepoint clusters accumulating per-glyph `getGlyphMetrics.advance` + `getGlyphKerning`, and `trackedWidth`/`trackedPrefixBytes` add the integer `dx` **between** clusters — this becomes the single width source feeding the tracking-aware `FontMeasurer`, so `wrap.wrapLines` (TEXT-02) and `wrap.ellipsisFit` (TEXT-03) inherit tracking **with zero changes** (they take only a `Measurer`). `draw`'s tracked path (`drawTrackedSpan`) blits each cluster at the cumulative advance the **same** `walkClusters` produces (each cluster rasterized alone — a single codepoint, so no ligature can form: the render-path half of the two-layer no-ligature defense, the NL asset being the other), so the rightmost pen equals the measured width and measure==draw cluster-for-cluster on **all** paths (single-line, wrapped, clip, ellipsis). Vertical metrics (baseline/lineSkip) are unaffected. **Cache keying:** the font backend `Fonts.at` already keys by whole **device** px; because sizes now arrive already multiplied by `scale`, a scale change opens the right sizes with no new map. The full render-affecting attribute set (device px, tracking, transform-applied bytes, wrap width, color, scale) is now defined and, per the slice boundary, TEXT-05's texture cache is **not** built — only `TextState.tracking` is added as POD so TEXT-05 can key on it. **Migration:** `style.h3`→21 (was 22); the `panel` title migrated from a body heading to the `eyebrow` role (uppercased, tracked section-heading — the representative uppercase eyebrow label, exercised by every panel across the mock/act/game pages); `mock.zig` showcases `heading`/`small`/`eyebrow` explicitly. **Tests (all deterministic/SDL-free):** `type.zig` — role contract (sizes/tracking/transform/weight), eyebrow-in-band, unstyled==body, `toDevice` identity-at-1/scale/floor, `deviceTracking` rounding, `applyTransform` ASCII fold/length-preserving/non-ASCII-passthrough/small-buffer-decline; `wrap.zig` — a **tracking-aware fake measurer** (1px/byte + fixed dx) proving positive tracking forces a break that fit untracked, negative tracking keeps a wider line, ellipsis prefix shrinks under tracking, and measure==draw determinism under tracking — proving the "feed a tracked measurer, wrap/ellipsis inherit it" leverage point; `text.zig` — `tracking` POD round-trip (default untracked fast path) and over-cap whole-refusal preserved with tracking set; `style.zig` — role fragments carry the contract, later-fragment override of tracking/transform, and in-place transform fold. `zig fmt`, `zig ast-check` (all changed files → 0), `zig build test-ui` (**221** tests, up from 200), `zig build test` (**250** tests, up from 229), and `zig build` all pass; zls reports no diagnostics on the changed files. Rendered-text texture caching (TEXT-05), `ViewMetrics`/real DPI scale (VIEW-01/02), and rotated collapsed-rail copy (TEXT-06) remain their own slices; `.kiro` untouched.
- [x] **TEXT-05 — Cache rendered text textures.** Avoid rasterizing every unchanged string every frame. Invalidate on content, font size, tracking, wrap width, color strategy, renderer reset, or scale change. Verify pool eviction releases textures and no per-frame GPU leak occurs while filtering catalogs.
  - COMMENT: Cached the uploaded glyph texture entirely host-side in `ui_client`, **mirroring the `svg` feature** (the codebase's proven resource-owning-state precedent) and leaving the generic engine (`src/ui`) texture-unaware — the `Pool` eviction hook (`cache.zig`) already frees a resource-owning state via its `deinit`, **unchanged**. Before this slice `features/text.zig`'s `drawSpan`/`blitCluster` ran `renderTextSolid → createTextureFromSurface → renderTexture → deinit` for **every visible string every frame**, so an unchanged label re-rasterized+re-uploaded+freed a GPU texture 60×/s and every catalog-filter keystroke re-rasterized the whole visible result set. `TextState` (`ctx_binding.zig`) stops being POD and gains an **optional cached `tex: ?sdl.render.Texture`** plus the `cache_key` that produced it (exactly `SvgState.tex`+`src_key`, generalized), a `deinit` that frees the texture, and an `abandonTexture` that drops the handle **without** freeing (the reset path). The SDL texture is a thin handle (memcpy-safe by value), so it survives pool growth/relocation exactly like `SvgState` — proven SDL-free by a new handle-survives-growth test. The TEXT-01..04 fields become **pure key inputs**, not behavior changes: TEXT-01 whole-refusal (`text()` null ⇒ no key, no texture, and any lingering texture freed while the renderer is alive), TEXT-02 wrap, TEXT-03 clip/ellipsis geometry (`remeasure` still stamps the allocated cell width; only the blit is cached — the clip **rect** stays a per-frame render-time op), and TEXT-04 tracking/scale are all preserved. **The pure seam:** all the parts that need no SDL live in a new **`features/text_cache.zig`** — the `Key` (every render-affecting dimension), `keyOf`/`Inputs` (fold a `TextState`+generation into a key, `null` for a refused/empty string), and `decide(stored, has_texture, wanted)` returning `.none`/`.hit`/`.miss{free_old}` — so the whole lifecycle is unit-testable **deterministically and SDL-free**, matching the `wrap.zig` fake-measurer and `Pool` tally-counter culture. **The enumerated key** folds: transformed accepted content bytes (Wyhash — the uppercase eyebrow transform is already folded in-place into the buffer by `style.apply`, so hashing the accepted bytes captures it for free); device px (`st.px`, already `logical*scale`, so a **scale** change is captured transitively, never double-counted); a `font_id` identity/weight token (a constant today, keyed now so multi-face/weight later invalidates); tracking; overflow mode + overflow_width (the ellipsis path's pixels are prefix+token, so the cell width must be keyed); wrap_width (break spans are a pure function of already-keyed inputs); and the **renderer generation** (see reset, below). **Color strategy = tint-on-blit:** glyphs are rasterized **white** and tinted at blit via `setColorMod`/`setAlphaMod` (the `svg.draw` model), so color is **not** a key dimension and a hover/focus recolor reuses the same texture with **zero** cache churn — the fixed strategy *is* the "color strategy" the slice keys on. **Scope (all variants cached):** **every** accepted variant — the single-line untracked fast path (the churn source — catalog cells, tokens, labels), tracked single-line, wrapped (tracked or not), `.clip`, and `.ellipsis` (including tracked wrapped/ellipsis) — rasterizes into **one composite white texture per `TextState`** on a cache miss and blits the tinted composite thereafter; **no** variant runs `renderTextSolid` on a hit. The composite is generated **atomically** in `features/text.zig`'s `renderComposite`: a fresh `.target`-access texture (`.blend`) is bound, the prior renderer target/clip/draw-color/blend are snapshotted and **always** restored (success or error path), the target is cleared transparent, and each variant's white sub-spans are stamped at the exact composite-relative offsets the pre-cache per-frame paths used — driven by the *same* `planVariant` walk that measures the composite bounds from both logical advances and actual per-span raster dimensions, so pixels match measure/draw glyph-for-glyph without cropping font-height or overhang differences (UTF-8 clusters, positive/negative tracking, multiline `lineSkip`/blank lines, ellipsis fit, clip full-source pixels). The `.clip` cell's clip **rectangle** stays the one per-frame render-time op (its cached pixels are the whole string; the rect is applied around the cached blit and restored, prior clip never dropped). On any generation failure the partial target is freed and **no** key is latched, so the next frame retries — a failure is never cached (composite generation is atomic). **Renderer reset & teardown (the correctness core):** added a monotonic `Platform.generation: u32` (`res.zig`) bumped by `bumpGeneration`, which `main.zig`'s event loop now advances on `render_targets_reset`/`render_device_reset`/`render_device_lost` (previously **unhandled** — blitting a texture from a reset device is UB / a leak). The generation is in the key, so a post-reset texture can never match and be blitted. The **free-vs-abandon** rule lives in one place (`text_cache.decide`): an ordinary invalidation (content/px/tracking/… changed, renderer alive) frees the old texture (`deinit`), but **any renderer-generation mismatch** — even when content or another key field also changes during the reset frame — means the old GPU texture already died with the device, so the handle is **abandoned** (`abandonTexture`, no `deinit`) to never double-free an already-invalid object, then re-rasterized under the new generation. Teardown order is already correct: `App.deinit` frees the ui pools **before** `renderer.deinit()`, so `TextState.deinit` always runs while the renderer is alive (a normal free, never a post-reset destroy-after-invalidation). **Failure/retry:** a failed `renderTextSolid`/`createTextureFromSurface`/`getStringSize` leaves **no** partial owned state (the surface is always freed, the texture freed if the later measure fails) and latches **no** key, so the next frame retries — never caching a failure, mirroring the existing `catch return` cosmetic-skip policy. **Deterministic fake-backend tests** (`text_cache.zig`, SDL-free, wired into `test_ui.zig`): a `FakeBackend` tallies `raster_count`/`destroy_count` and hands out generation-tagged sentinel textures; a `FakeSlot` (run through a **real `Pool(T)`**) mirrors the ownership rules. Coverage — key folds every dimension and ignores color; `decide` none/first-miss/hit/free-on-content/abandon-on-reset; **unchanged label rasterizes+uploads exactly once across 120 frames** (the TEXT-05 proof, `destroy_count==0`); content change invalidates exactly once (old freed once, new rasterized); **renderer reset abandons (never frees) the stale texture even when content changes in the reset frame and re-rasterizes under the new generation**, the new texture carrying the new generation; rasterize failure latches no key and the next frame retries; `Pool` prune frees a slot's texture exactly once; reuse of a pruned hole re-inits clean (no stale texture, no double free); teardown frees each live slot once; texture handles survive pool growth; and the **catalog-filter integration/performance test** — M row-label slots, a filter narrowing to a subset over many frames, asserting the filtered-out rows are freed **exactly once each** and the survivors keep a **single** texture (no per-frame re-raster, no destroy churn). `zig fmt`, `zig ast-check` (all changed files → 0), `zig build test-ui` (**242** tests), `zig build test` (**271** tests), and `zig build` all pass; zls reports no diagnostics on the changed files (`res.zig`, `main.zig`, `test_ui.zig`, `ctx_binding.zig`, `features/text.zig`, `features/text_cache.zig`). New per-variant coverage in `text_cache.zig` proves — SDL-free, through the fake backend and a real `Pool` — that **every** variant (normal, tracked, wrapped, tracked-wrapped, clip, ellipsis, tracked-ellipsis) rasterizes its composite **exactly once across 120 frames** (`destroy_count == 0`), invalidates **exactly once** on its own pixel-affecting key change (tracking / wrap-width / ellipsis cell-width / clip content), switches variant on a reused node without a stale hit, generates the composite **atomically** (a failed generation latches no key and retries, on every variant), abandons-then-re-rasterizes each variant's composite on a renderer reset (never a double-free), and keeps one composite per variant across a 120-frame mixed-variant catalog. Real DPI/`ViewMetrics` scale (VIEW-01/02) and rotated collapsed-rail copy (TEXT-06) remain their own slices; `.kiro` untouched; no commit/push.
- [x] **TEXT-06 — Support rotated/vertical collapsed-rail copy.** Either add a transformed texture/text draw payload or implement a deliberate stacked-glyph equivalent with matching focus/hit bounds. Do not rotate layout geometry without rotating rendering and hit expectations together.
  - COMMENT: Added a rotated-text payload entirely host-side in `ui_client`, leaving the generic engine (`src/ui`) orientation-unaware and reusing the TEXT-05 upright composite unchanged. `TextState` (`ctx_binding.zig`) gains a bounded `Orientation = enum { horizontal, counter_clockwise_90 }` plus `orientation: Orientation = .horizontal`. The bounded enum is deliberate over an arbitrary angle: 90° is an exact width/height **swap**, so the rotated footprint stays axis-aligned and needs no non-rectangular hit-geometry policy (the roadmap's "do not add generic-engine rotation or arbitrary angles"). `El.with_orientation(...)` sets it — clearing the incompatible single-line `wrap_width`/overflow-cell constraints first, since the rail label is one line — and re-measures; `El.vertical()` is the convenience spelling for the collapsed-rail counter-clockwise mode. **Rendering, layout, focus, and hit geometry agree by construction, never rotated independently.** `features/text.zig`'s `remeasure` was refactored to compute upright `Metrics` once (single-line/tracked/wrapped/cell branches unchanged) and then apply one pure `orientMetrics(orientation, upright)` transform: `.horizontal` keeps the metrics; `.counter_clockwise_90` swaps width↔height and zeroes the baseline (a vertical control label does not participate in horizontal row-baseline alignment). The engine stamps that swapped node box, so the layout box, the focus outline, **and** the rectangular hit target are the rotated (tall) footprint — the same box the pixels occupy. **Orientation is a final-blit transform, not a composite-pixel input**, so it is deliberately **not** a TEXT-05 cache-key dimension: the glyphs rasterize into the same upright white composite as horizontal text, and switching a node between horizontal and vertical reuses the cached texture with zero glyph re-raster/re-upload (proven by leaving `text_cache.Inputs`/`Key` untouched). `drawCached` now receives the full content `box` (not just an origin) and passes the orientation to `blitCached`, which computes a pure `blitPlacement`: horizontal draws the upright `w×h` texture at the box origin; counter-clockwise centers the unrotated `w×h` destination on the swapped `h×w` box and draws it with SDL's `renderTextureRotated(tex, null, dst, 270, null, .{})` — 270° clockwise = 90° counter-clockwise, reading **bottom-to-top** — about its own center, landing the final pixels exactly on the oriented box. (`renderTextureRotated`'s `flip_mode` parameter is `surface.FlipMode`, a `{horizontal,vertical:bool}` struct, so "no flip" is `.{}`.) The `.clip` cell path still scopes/restores the renderer clip around the (now oriented) blit. Development fixture: `pages/mock.zig` builds a 36×124 collapsed-rail stand-in (a fixed-size bordered `panel`-toned column) with a horizontal restore chevron (`\u{203A}`) above the label `HOLDINGS` set via `.vertical()` at the prototype's collapsed-rail density — a 9px font with loosened `+0.09em` tracking; the rotated label's box/focus/hit is the strip's inner column. Present-tense docs updated in the same slice: `src/ui_client/README.md` gains a "Rotated collapsed-rail copy (TEXT-06)" subsection (orientation API, axis-swapped layout/focus/hit geometry, final-blit rotation, upright-texture reuse / no orientation cache invalidation) and the inline `TextState` description now names `orientation`. Added deterministic SDL-free tests to `features/text.zig` (pulled into `test-ui`): orientation defaults horizontal and preserves accepted content across a mode switch; `orientMetrics` keeps horizontal metrics and swaps axes + zeroes baseline for counter-clockwise; `blitPlacement` centers the upright texture on the swapped box, sets 270°, and — verified against the box bounds — the center rotation reproduces exactly the oriented box; and the horizontal blit remains the existing origin-sized, unrotated path. `zig fmt`, `zig build test-ui`, and `zig build` all pass; a real SDL screenshot of the mock rail confirms `HOLDINGS` reads bottom-to-top with no clipping/mirroring, the rotated text fits the 36px-wide strip, and ordinary horizontal text is unchanged. Boundary: only the two axis-aligned orientations exist (no clockwise/180°/arbitrary angle); the collapsed rail itself (KIT-05) and its transition/memory remain their own slices — this provides the rotated copy primitive they consume.

## 6. Rendering capabilities — `src/ui_client/features/`

- [x] **RENDER-01 — Verify alpha blending end to end.** Solid fills, text, SVG/image tint, geometry, and overlays must honor alpha consistently. Configure SDL blend modes explicitly and add a visual fixture for translucent row hover, backdrop, locked tiles, and gradient washes.
  - COMMENT: Root cause confirmed by inspection: the SDL renderer's **draw blend mode was never configured**, so it stayed at SDL's default `.none` (source *replaces* destination, ignoring `a`). Every geometry primitive the paint features emit — `fill`/`outline`/`line` via `renderFillRect`/`renderLines` — therefore rendered a translucent color (`a < 255`) **opaque**; nothing looked wrong only because every production color is currently opaque (`a = 255`). A translucent hover row, modal scrim, dimmed/locked tile, or wash would have painted solid. Fix: configure blend modes **explicitly** rather than relying on any default. (1) `main.zig` sets `renderer.setDrawBlendMode(.blend)` once at renderer creation (after `setVSync`), so the initial `clear` and any pre-walk draw share a known baseline. (2) The host render walk `draw.zig`'s `draw_tree` re-asserts `setDrawBlendMode(.blend)` at the top of **every** tree — the architectural home for a render-wide invariant (it already owns the clip-stack reset), and per-tree so the baseline is **self-healing** after a renderer/device reset rather than silently lost. This is the right layer: `src/ui` stays SDL-unaware, and the policy lives in `ui_client`. (3) Texture features now set their blend mode **explicitly** before their tinted blit instead of depending on a texture's creation default: `svg.draw` and `image.draw` call `texture.setBlendMode(.blend)`, and the cached `text` composite blit (`blitCached`) does too (the composite is already generated `.blend` in `renderComposite`, but the blit sets it explicitly so a dimmed/translucent label composites consistently). The text compositor's render-target pass (`renderComposite`) already snapshots `getDrawBlendMode` and restores it, so the new `.blend` baseline is preserved across compositing — verified by reading that path. Overlays inherit correctness for free: `modal`/`tooltip` are ordinary `fill` nodes, so a translucent scrim will now blend when one is authored (the production modal scrim currently uses opaque `theme.bg` — a translucent backdrop is deliberately **KIT-08** scope, not changed here; RENDER-01 provides the capability). **Honest scope boundary:** the roadmap line names "geometry" and "gradient washes", but the geometry feature (RENDER-03) and linear-gradient composition (RENDER-04) do not exist yet — so the fixture's wash is honestly a **stack of translucent bands**, not a true gradient, and the `line` feature (the only present geometry primitive) was covered by the same `.blend` baseline. **Fixture** (`pages/mock.zig`, a "Blending" panel on the dev showcase, plus SDL-free `layer`/`alpha` helpers): (1) a translucent-accent row-hover tint over an opaque panel band, (2) a translucent dark backdrop scrim half-covering a bright accent block (reads darker, not black), (3) a `warn` locked tile dimmed by a translucent `bg` overlay (shows through at reduced opacity), and (4) an eight-band translucent-white wash building a left-to-right brightening ramp purely through alpha compositing — the strongest visual proof, since with `.none` every band would be a single flat color. Colors carry alpha in the `Color.a` byte (`Color` = SDL `pixels.Color`), so translucency is authored, not a new feature. **Validation:** `zig fmt --check`, `zig ast-check` (all changed files), `zig build test-ui`, `zig build test`, and `zig build` all pass; LSP reports no diagnostics; `git diff --check` clean. A real SDL screenshot of the mock "Blending" panel confirms the row-hover tint, the darkened (not black) scrim overlap, the dimmed-but-visible locked tile, and the stepped brightening wash all alpha-composite correctly, proving fills honor alpha end to end (with the same baseline covering `outline`/`line`, and the texture paths honoring tint alpha). Present-tense docs updated in the same slice: `src/ui_client/README.md`'s render-walk section gains an "Alpha-blending policy (RENDER-01)" paragraph. `src/ui` untouched; `.kiro` untouched.
- [x] **RENDER-02 — Extend style payloads without turning style into placement.** Add text tint/size/tracking/wrap/overflow and relevant SVG/image tint fields while preserving last-fragment-wins behavior. Placement remains imperative through `El`/layout.
  - COMMENT: Resolved by reconciling the roadmap line with the deliberate style/placement split this layer already enforces. **Text tint/size/tracking were already style** — TEXT-04 added `Style.text` (glyph ink), `Style.font` (logical size), `Style.tracking` (em), and `Style.transform` (case), all folding last-non-null-wins through `resolve`/`apply`; nothing to add there. **`wrap`/`overflow` are deliberately kept out of `Style`.** They set a text node's *measurement constraint* (its reserved layout/hit box), which is placement, not look — TEXT-02/03 established them as imperative on `El` (`with_wrap`/`with_cell`) with a documented rationale, and moving them into `Style` would be *exactly* the "turn style into placement" this task forbids ("Placement remains imperative through `El`/layout"). So they stay imperative, and a later `with_style(.{ font })` re-measures at whatever constraint `El` already set — the invariant is preserved, not duplicated into a second vocabulary. **The real gap was SVG tint**: `Style` had no way to recolor a texture aspect, so `pages/templates/action_tile.zig` reached past the fold and assigned `icon.get().render_data.svg = …` directly (its own comment read "`Style` has no svg-tint slot (yet)"). Added `Style.tint: ?Color` — *texture-content ink* — and routed it in `apply`: when `node.render_data.svg != null`, a set `tint` recolors it, folding last-non-null-wins like every other field. `tint` is a **separate field from `text`** on purpose (glyph ink vs texture ink), so one tuple can carry both without either shadowing the other, and `tint` no-ops harmlessly on a node with no svg. The SVG raster is rasterized white and tinted at blit (the `svg.draw`/`text`-composite model), so this is a look applied at draw with zero re-raster, consistent with RENDER-01's tint-on-blit. **Assert relaxed correctly:** `apply`'s non-text branch previously asserted *all* of `text`/`font`/`tracking`/`transform` are null; the svg-tint routing now runs before the text branch, and the inert-typography assert still guards only the genuinely-inert typography set (a node with neither text nor a matching aspect) — `tint` is not typography and is explicitly allowed on a non-text node. **Migration:** `action_tile`'s icon now uses `el.svg(...).with_style(.{ Style{ .tint = if (lit) th.dim else chrome } })` — same colors as before (`th.dim` when lit, the `chrome` value otherwise), so **no production visual change**, just the tint flowing through the fold instead of a raw poke. Image tint was considered and deliberately **not** added: `render_data.img` is a `Sprite` (texture + optional sheet cell), not a color payload, so there is no "relevant" image *tint field* to add without changing the img payload — out of scope for this slice and unused by any consumer; SVG is the relevant texture-tint aspect and it is covered. **Tests (SDL-free, in `style.zig`, run in `test-ui`):** `tint` folds last-non-null-wins independently of `text` (a tuple sets both; a later fragment overrides only `tint`); `apply` recolors a present svg aspect and is inert (no assert trip) on a node with no svg; and tinting an svg node leaves glyph-ink `text` untouched (orthogonal fields). These use the same `apply(undefined, node, …)` pattern as the existing decoration tests (the svg branch dereferences no `ctx`). **Validation:** `zig fmt --check`, `zig ast-check` (changed files), `zig build test-ui`, `zig build test`, and `zig build` all pass; LSP reports no diagnostics; `git diff --check` clean; a real SDL screenshot of the ACTIONS tiles confirms the icon tint is unchanged (same dim/chrome colors, now via the fold). Present-tense docs updated in the same slice: `src/ui_client/README.md`'s Style section documents the `tint` field and records the wrap/overflow-stays-placement decision. `src/ui` untouched; `.kiro` untouched.
- [x] **RENDER-03 — Add a geometry feature backed by `renderGeometry`.** It must draw untextured colored triangles/polygons and honest thick polylines with correct joins/caps. This is shared by hex bodies/rails/markers, distribution curves, slider diamonds, and any diagonal indicator.
  - Add pooled variable-length vertex/index state with explicit capacity/ownership and eviction.
  - Test convex hex fill, mixed-color triangles if used for gradients, closed rails, acute joins, clipping, alpha, and scale.
  - Replace the current first-segment-normal thick-line approximation after parity consumers migrate.
  - COMMENT: Added a `geometry` paint feature backed by SDL's `renderGeometry`, drawing untextured **per-vertex-colored** triangle meshes — a convex-polygon fill and an **honest thick polyline** with correct miter joins and butt/square caps — as one more module in the existing feature seam (one `features/` module + one `list` entry + one `RenderData` field; `assertFeature` stays green). **Pure tessellation, SDL-free (`features/geometry_tess.zig`):** the geometry math lives in its own module that imports only `std` — plain `V2`/`Rgba`/`Vertex` types (no host/SDL types) and a bounded `Mesh` builder over caller-owned fixed slices. `fillConvex`/`fillConvexMulti` fan-triangulate a convex polygon from vertex 0 (`n` points → `n−2` triangles sharing the fan center; `fillConvexMulti` carries per-vertex colors for a mixed/gradient fill — the RENDER-04 seam). `strokePolyline` expands a polyline to `half_width` with **miter joins** at interior/looped vertices (offset along the angle-bisector at distance `half_width/cos(θ/2)`, clamped to a **bevel fallback** past a `4.0` miter limit so an acute spike can't run to infinity), and **butt/square caps** at open ends (square extends each end by `half_width` along the segment); `closed` connects last→first for a rail loop. The `Mesh` builder **refuses an over-capacity mesh as a whole** (latches `overflow`, emits no partial triangle), so an over-budget shape draws nothing rather than a torn mesh — the whole-refusal policy shared with `TextState`/`LineState`. **Pooled variable-length state with explicit capacity/ownership/eviction (`GeometryState` in `ctx_binding.UiState`):** the second variable-length state after `LineState` (a mesh's vertex/index count doesn't fit in a `RenderData` tint payload). It holds inline `verts: [vcap=256]Vert` + `idx: [icap=512]u16` with lengths, is **POD** — owned by value, no allocator, **no `deinit`** — because the mesh is CPU-side vertex data uploaded per frame by `renderGeometry`, not a retained GPU texture; ownership is by-value and eviction reclaims the slot with nothing to free, so the `Pool` init-on-fresh/reuse contract holds unchanged. `set` copies a tessellated mesh or refuses it whole on capacity overflow. Positions are **node-local unit-square** coords (new `Vert = { p: Point, color: Color }`), like `line`, so a shape survives resize/zoom. **The feature (`features/geometry.zig`):** `Payload = ?Geometry` carries only a per-mesh `opacity` multiplier (the parameters that are *not* the variable-length vertices — the RENDER-07 dimming hook); `attach_polygon`/`attach_polyline` run the tessellator once at build into the pooled state; `draw` maps each unit-square vertex to device px through the node's laid-out box, converts each per-vertex `Color` (u8) to SDL's float `FColor` (0..1) with `opacity` folded into alpha, and submits one indexed `renderGeometry(null, verts, idx)`. **Alpha, clipping, scale come for free** from the render walk: RENDER-01's `.blend` baseline makes vertex alpha composite, the walk's clip stack crops the triangles like any paint, and the unit-square→box mapping means a resized/zoomed/DPI-scaled node just remaps. `El.polygon`/`El.polyline` are the fluent builders; `feat.data_polygon`/`data_polyline` the mixin aliases. **z-order:** inserted as `{ fill, image, svg, geometry, line, text, outline }` — geometry above raster content, below the polyline/text/outline layers. **Tests (all SDL-free, in `test-ui`; 262 total, up from 242):** in `geometry_tess.zig` — hexagon fans into 4 triangles all sharing vertex 0; `<3` points is a no-op; per-vertex colors preserved (mixed-color/gradient); over-capacity refuses the whole mesh (no torn triangle); a straight segment is a rectangle of the right width; a square cap extends both ends by `half_width`; an acute right-angle join miters to the bisector distance `half_width/cos45°≈2.828`; a closed square loop strokes all four edges including last→first (16 verts / 8 triangles); `<2` points or zero width is a no-op. In `geometry.zig` — `Color`→`FColor` with opacity folded into alpha only; the unit-square→device box mapping (the resize/zoom/scale contract, pinned without a renderer); and `toV2` reinterpreting `Point` as `V2` without a copy. A real SDL screenshot of a temporary mock demo (a filled hexagon + a thick closed rail + an acute polyline) confirmed the fill, the miter joins, the caps, and vertex alpha render correctly on-GPU. **Deliberate boundary — `line.zig` not retired:** the roadmap says to replace the first-segment-normal thick-line approximation *after parity consumers migrate*; no consumer has migrated yet (the board doesn't exist), so `line.zig` stays as-is and this feature is additive. **Validation:** `zig fmt --check`, `zig ast-check` (all changed files), `zig build test-ui`, `zig build test`, and `zig build` all pass; LSP reports no diagnostics; `git diff --check` clean. Present-tense docs updated in the same slice: `src/ui_client/README.md`'s Paint-features section gains a `geometry` description and the state-registry enumeration lists `GeometryState` as the second variable-length state. `src/ui` untouched; `.kiro` untouched.
- [x] **RENDER-04 — Add linear-gradient composition.** Support the eating-slider split track and milestone/state washes, either as geometry vertex colors or a narrow gradient feature. Required orientations and stops should be explicit; a full CSS gradient grammar is out of scope.
  - COMMENT: Implemented as **explicit gradient composition on the RENDER-03 geometry feature** (vertex colors, not a second feature), keeping the surface narrow. **The prototype's gradients (grepped `prototype.css`) are two shapes:** the eating-slider split track — `linear-gradient(90deg, acc 0 <pos>, line-2 <pos> 100%)`, a **hard two-color split** at a position — and the milestone/state washes — `linear-gradient(90deg, rgb(tint / 8–11%), transparent 72–76%)`, a **tint→transparent fade**. Both are `90deg` (horizontal); the one `145deg` terminal-background wash is RENDER-05 backdrop territory and diagonal, so it is out of RENDER-04's named scope. **Pure tessellation (`geometry_tess.zig`):** added `Dir { horizontal, vertical }`, `Stop { pos, color }`, and `fillGradient(mesh, dir, stops)` — between each consecutive pair of ordered stops it emits a quad whose four corners carry the two stops' colors (both corners at a stop share that stop's color, so `renderGeometry` interpolates only *along* the axis; the cross axis spans the full 0..1). This one routine covers all three cases with **explicit** stops: a **smooth ramp** (distinct positions/colors), a **hard split** (two stops at the *same* position — the middle span is a zero-area degenerate quad that paints nothing, so the edge is crisp with no blend across it), and a **wash** (a `tint → a=0` pair; the transparent end composites over the base via the RENDER-01 `.blend` baseline). Fewer than 2 stops is a no-op; the `Mesh` builder's whole-mesh overflow refusal applies. It is deliberately **not** a CSS gradient grammar — no angles beyond the two axes, no implicit stop positions, no color-space options. **Feature + El:** `features/geometry.zig` gains `Dir`/`GradientStop` re-exports and `attach_gradient(ctx, node, dir, stops, opacity)` (converts host `Color` stops → tessellator `Rgba`, runs `fillGradient` into the pooled `GeometryState`, reuses the existing `draw` — unit-square → device-px mapping, `Color`→`FColor`, mesh `opacity` folded into alpha). `El.gradient` is the fluent builder; `elements.zig` re-exports `GradientStop`/`GradientDir` so screens name the vocabulary; `feat.data_gradient` is the mixin alias. Reuses the RENDER-03 `GeometryState` pool, `renderGeometry` draw path, `.blend` baseline, and clip stack unchanged — the gradient is just a different mesh. **Tests (SDL-free, 5 new in `geometry_tess.zig`; 267 test-ui total):** a two-stop horizontal wash is one quad with the opaque tint at x=0 and transparent at x=0.74 (cross axis spans 0..1); a duplicated-position split yields 3 spans (12 verts/18 idx) with every vertex left of the split `acc` and every vertex right `line2` (hard, no blend); a vertical ramp varies color down y with x spanning 0..1; `<2` stops is a no-op; over-capacity refuses the whole mesh. **Deliberate boundary — no consumer migrated:** the named consumers don't exist yet — the range slider is KIT-10/KIT-19 and the milestone wash is KIT-20 — and the current `ration_dial` uses discrete chips, not the split-track slider. So, exactly like RENDER-03 with the board, this ships the **primitive with explicit orientations/stops** and defers wiring; a real SDL screenshot of a temporary mock demo (a wash fading to transparent at 74%, a hard `acc`|`line2` split track, and a smooth `acc → danger` ramp) confirmed all three render correctly on-GPU with the transparent end compositing over the base. **Validation:** `zig fmt --check`, `zig ast-check` (changed files), `zig build test-ui`, `zig build test`, and `zig build` all pass; LSP reports no diagnostics; `git diff --check` clean. Present-tense docs updated in the same slice: `src/ui_client/README.md`'s geometry description gains a "Linear-gradient composition" paragraph. `src/ui` untouched; `.kiro` untouched.
- [x] **RENDER-05 — Add reusable shadow/backdrop composition.** Cover the centered terminal shadow, dialog shadow, popup shadow, and modal backdrop with bounded layers or a simple shadow feature. Match the understated prototype; do not introduce rounded cards or glossy effects.
  - COMMENT: Added `ui_client/shadow.zig` — reusable shadow/backdrop composition as **bounded translucent layers** (the roadmap's stated option), because SDL has no blur primitive. The prototype's four shadow tokens (grepped `prototype.css`) are soft drop shadows — terminal `box-shadow: 0 16px 80px #000a`, dialog `0 18px 80px #000d`, popup `0 4px 14px #0008`/`0 7px 24px #000b` — plus the modal `.terminal-dialog::backdrop { background: #090806d9 }`. **Pure layer math (SDL-free, tested):** `layers(Spec, out)` turns a prototype-style `Spec { offset_x, offset_y, blur, base_alpha, count }` into an ordered, bounded set of `Layer { spread, dx, dy, alpha }` — outermost widest/faintest (`spread = blur·(count−i)/count`) through inner tightest/darkest, each carrying the full drop offset, and alpha ramped toward the core **and divided by `count`** so N faint layers *sum* to the intended darkness rather than each being opaque (kept understated). Edge cases: `count` clamped to `1..max_layers` (8); a non-positive `blur` collapses to a single hard shadow at the offset. **Thin `El` emitter:** `drop(ctx, parent, x, y, w, h, spec)` places the layers as absolutely-anchored fill rects grown by each layer's spread and offset, keyed `"{id}_sh{i}"`; call it **before** building the box so the layers paint under it (siblings paint in child order). Flat black at per-layer alpha; the RENDER-01 `.blend` baseline is what composites them (no shadow reads right without it). `backdropColor(ground)` returns the near-black terminal ground at the prototype's `0xd9` scrim alpha for the fullscreen root a modal already builds. **Chose a `ui_client` composition helper over a paint *feature*** because a shadow paints *outside/behind* a node's own box (multiple sibling rects), which the feature model (one aspect painted within/around a node) does not express; a helper that emits ordinary fill nodes composes cleanly and reuses the existing `fill` feature + clip/blend machinery with no engine change. **Understated, per the constraint:** flat black translucency only — no rounded corners, no gloss, no glow. **Deliberate boundary — no consumer wired:** the named consumers do not exist yet — the centered terminal is VIEW-03, the dialog/popup shadows are KIT-08/09, the modal backdrop is KIT-08 (the current `widgets.modal` still uses an opaque `theme.bg` scrim) — so, consistent with RENDER-03/04, this ships the tested primitive and defers wiring. A real SDL screenshot of a temporary mock demo (a panel box with a soft drop shadow behind it) confirmed the bounded layers build an understated soft penumbra that composites correctly over the background. **Tests (SDL-free, 5 in `shadow.zig`, pulled into `test-ui`):** ordered layers with outermost widest/faintest and inner tightest/darkest and every layer carrying the drop offset; per-layer alpha stays below the base (no opaque layer); `count` clamped to `1..max_layers`; a zero blur is a single hard shadow at the offset; `backdropColor` keeps the ground rgb and sets `0xd9` alpha. **Validation:** `zig fmt --check`, `zig ast-check`, `zig build test-ui`, `zig build test`, and `zig build` all pass; LSP reports no diagnostics; `git diff --check` clean. Present-tense docs updated: `src/ui_client/README.md` gains a "Shadow / backdrop composition" section, and the module is registered in `ui_client/root.zig` + `test_ui.zig`. `src/ui` untouched; `.kiro` untouched.
- [x] **RENDER-06 — Preserve crisp logical hairlines at scale.** One-logical-pixel borders, dashed/dotted outlines, rails, and focus rings remain legible on high-DPI displays. Define rounding/snapping so adjacent catalog columns and hex rails do not shimmer.
  - COMMENT: Added a centralized, pure hairline-snap policy in `features/paint.zig` (the shared paint-geometry module every feature `draw` already uses) and applied it to the hairline consumers. **The problem:** layout geometry is device-space f32; a 1-logical-px line at a fractional DPI scale (e.g. `1 × 1.5 = 1.5px`) or landing on a fractional device coordinate anti-aliases into a blurry 1–2px smear, and adjacent columns/rails at fractional positions **shimmer** as their sub-pixel coverage shifts frame to frame. **The policy (two pure helpers):** `hairline(logical_w, scale)` = `@max(1, @round(logical_w · scale))` — a hairline width is a whole number of device px, floored at 1 (never a fractional smear, never invisible; `@round` not `@ceil` so a sub-pixel authored width doesn't double; non-positive scale treated as 1); `snap(device_coord)` = `@round(coord)` — a hairline edge sits *on* a pixel boundary. **Applied:** `outline.draw` (borders, focus rings — the `1px warn focus-visible` ring is an outline, and dashed/dotted segments derive from the same snapped width) now snaps its four box edges to whole pixels and its bar thickness through `hairline`, replacing the old `@max(o.width, 1)`; `line.draw` (rails/polylines) snaps its stroke width through `hairline`, replacing `@max(1.0, stroke.width)` (the `w <= 1` single-`renderLines` fast path is preserved). **Consistent with the existing seam:** `type.deviceTracking` already rounds letter-spacing to integer device px "so hairlines stay crisp (RENDER-06)"; this generalizes that discipline to strokes, all routing through the one `View.scale` VIEW-01 will feed. **Today `scale` is 1**, so both helpers are identity for integer-authored geometry and production rendering is byte-unchanged — a real SDL screenshot of the Act I HUD confirmed the button and log-panel borders are still crisp 1px lines with no regression; the policy becomes load-bearing only when VIEW-01 supplies a fractional factor, which is why it is defined now (forward-looking correctness, like `deviceTracking`). Geometry-feature hex rails (RENDER-03) draw in unit-square coordinates mapped to the box, a different snapping concern deferred with the board consumer; the axis-aligned hairlines that actually shimmer today (borders/columns/focus rings) are covered. **Tests (SDL-free, in `paint.zig`, pulled into `test-ui`):** `snap` rounds to whole pixels; `hairline` gives ≥1 crisp device px at scale 1/1.5/2/3, floors a downscaled thin line at 1, scales a thicker line, and treats a non-positive scale as 1. **Validation:** `zig fmt --check`, `zig ast-check`, `zig build test-ui`, `zig build test`, `zig build` all pass; LSP clean; `git diff --check` clean; production HUD screenshot verified unchanged. Docs updated in `src/ui_client/README.md` (a "Crisp hairlines at scale" note in the render-walk section). `src/ui` untouched; `.kiro` untouched.
- [x] **RENDER-07 — Add per-node visual opacity/tint composition where it removes duplication.** Board filter dimming and disabled states should not require recomputing every child color independently. Opacity must not change hit eligibility; state logic decides that separately.
  - COMMENT: Added an **inheritable per-node visual opacity** applied in the render walk, so a whole subtree dims at once without recomputing each child's color. `RenderData` gains a plain `opacity: f32 = 1` field — deliberately **not a paint feature** (no `list` entry, no `assertFeature` change; features are aspects painted *within* a node, opacity is a walk-level modulation over the node *and its descendants*). `draw.draw_node` now carries an inherited `opacity` argument beside the clip stack: it computes `eff = inherited · node.render_data.opacity`, folds `eff` into **every** feature's paint alpha, and passes `eff` down to the children — so one `El.with_opacity(0.4)` on a board tile or a disabled control dims its fill, outline, text, svg, image, line, and geometry together, and nothing downstream re-derives a color. **The fold points:** a single pure helper `paint.applyOpacity(color, opacity)` scales a `Color`'s alpha (identity fast path at `opacity ≥ 1`, clamped), used by `fill`/`outline`/`svg`/`line`/`text` (text folds it into its tint-on-blit color, so a dimmed label reuses the same cached composite — zero re-raster); `img` folds it into `setAlphaMod`; `geometry` multiplies it into its existing per-mesh payload opacity before the `FColor` conversion. Every feature `draw` signature gained a trailing `opacity: f32` (mechanical, 7 features), keeping opacity a render-walk concern rather than a per-widget recompute. **Opacity never touches hit eligibility:** `render_data.opacity` is read *only* in `draw_node` (the paint walk); the generic engine's `mark`/interaction path stores `RenderData` opaquely and never inspects it, so a fully-dimmed node is exactly as clickable as its `ControlState`/predicate says — dimming and disabling are orthogonal, as the roadmap requires (a disabled control publishes `disabled` for hit/semantics *and* may dim visually, but the dim is not what disables it). The RENDER-01 `.blend` baseline is what makes the reduced alpha composite. `El.with_opacity(x)` is the fluent setter. **Tests (SDL-free, in `paint.zig`):** `applyOpacity` scales alpha only (rgb preserved), `opacity ≥ 1` is identity, `0`/negative clamps to transparent, `0.5` halves alpha. **Screenshot:** a temporary mock demo of three identical subtrees (bordered panel + accent text + warn chip) at opacity 1.0/0.5/0.2 confirmed each dims as a whole with the progressive fade visible and no per-child recolor. **Deliberate boundary:** the named consumers (board filter dimming BOARD-13, disabled chrome) aren't built yet, so this ships the mechanism; existing controls still dim via their own color choices until they adopt `with_opacity`. **Validation:** `zig fmt --check`, `zig ast-check`, `zig build test-ui`, `zig build test`, `zig build` all pass; LSP clean; `git diff --check` clean. Docs updated in `src/ui_client/README.md` (a "Per-node visual opacity" note) and the `RenderData.opacity` field doc. `src/ui` untouched; `.kiro` untouched.
- [x] **RENDER-08 — Add small transition/tween state in the host layer.** Support the prototype's functional transitions: Holdings column/gap `120ms`, stock token font change `90ms`, and board opacity/stroke changes around `100–110ms`. Key animations by stable node/domain ID, interrupt/reverse cleanly, and obey reduced motion. No general timeline engine or decorative keyframes are required.
  - COMMENT: Added `ui_client/tween.zig` — a small **host-layer tween registry**, deliberately a value provider and **not** a general timeline engine or a node feature. A `Tween` is one scalar linear ramp `{ from, to, elapsed, duration }` with `value()` (clamped) / `done()` / `advance(dt)`. A fixed, non-allocating `Registry` (cap 64) keys tweens by a **stable `u64` node/domain id** — the same stable-key discipline the interaction/focus/semantic registries use, so a tween is not lost across reorder/filter/rebuild (a test drives two independent domain ids and a release). **Interrupt/reverse cleanly:** `retarget(id, to, duration)` on an *existing* tween restarts from its **current value** (a smooth reversal — a half-open Holdings rail glides back from where it is, not from the far end), and a repeated retarget to the same in-flight target does **not** restart; a *brand-new* id sits at its target instantly (a control's first appearance is its state, not a transition), and later retargets animate. `from(id, start, to, duration)` seeds an explicit initial ramp. **Obeys reduced motion** through the INPUT-10 seam: the registry holds a `motion.Policy` (projected from `Resources.motion` each frame) and, when reduced motion is on, every `retarget`/`from` sets the tween immediately to `to` — the transition **snaps** while the functional end state is always reached (this is exactly the `Policy.snap` gate INPUT-10 shipped "for RENDER-08"). **Prototype durations** are named constants in seconds: `holdings_s = 0.120`, `stock_token_s = 0.090`, `board_s = 0.105` (the 100–110ms band, mid). **Bounded/safe:** a full table drops a new id and the consumer's `value(id, fallback)` returns its target — the control shows its end state with no animation rather than crashing or allocating. **Wired, not just declared:** `Resources.tween` holds the registry inline (POD); `main.zig` advances it by the frame `dt` and projects the reduced-motion policy onto it **every frame before systems run**, independent of the sim clock (UI transitions animate even at the Act I curtain), using the raw `time.dt` (frame delta) rather than the pausable `sim.elapsed`. **Pure and SDL-free** (the caller supplies `dt`), so all behavior is unit-tested without a renderer or clock. **Tests (SDL-free, in `tween.zig`, pulled into `test-ui`):** linear interpolation clamped at the ends; instant on non-positive duration; new-id-sits-at-target then reverses from current on interrupt (mid-flight, no jump, tested through a double reversal); repeated same-target retarget doesn't restart; reduced motion snaps; unknown-id fallback and stable-key independence with release; full-table drop-to-fallback; and the durations equal the CSS ms in seconds. **Deliberate boundary — no consumer wired:** the named consumers don't exist yet — Holdings collapse is KIT-05, the stock token is KIT-12, the board states are BOARD-05 — so, like the rest of this RENDER batch, this ships the tested mechanism and its durations and defers the wiring; there is no on-screen animation to screenshot (a value provider), and no decorative keyframes were added. **Validation:** `zig fmt --check`, `zig ast-check`, `zig build test-ui`, `zig build test`, `zig build` all pass; LSP clean; `git diff --check` clean. Present-tense docs updated: `src/ui_client/README.md` gains a "Transition/tween state" section; registered in `ui_client/root.zig`, `res.zig`, `main.zig`, and `test_ui.zig`. `src/ui` untouched; `.kiro` untouched.

## 7. Frame-local view metrics and responsive layout

- [x] **VIEW-01 — Add `ViewMetrics` to `Resources.View`.** Compute once per frame from window pixel size, drawable size/DPI, and the `900×820` reference. Include logical scale, logical viewport, terminal rect, and width classes matching `760`, `560`, and `440` logical px breakpoints.
  - COMMENT: Added `ui_client/view.zig` — a **pure, SDL-free** `ViewMetrics` + `compute(logical_w, logical_h, pixel_density)`, computed once per frame in `build_ui`'s prologue into `Resources.view.metrics`, with `View.scale` set from `metrics.dpi_scale`. **Two independent scales, deliberately kept apart** (the crux of getting this right): the **DPI scale** (`dpi_scale` = drawable device px ÷ window-coordinate px, from `window.getPixelDensity()`) is only a *crispness* factor — it feeds `View.scale` so `type.toDevice` opens fonts at the right device size and the RENDER-06 hairline snap rounds to whole device px — and it does **not** change layout, because the layout is solved in **logical (window-coordinate) space** (`window.getSize()`), which is the space SDL's renderer already maps to pixels; the **responsive width class** is derived from the *logical* width against the prototype's `760/560/440` breakpoints, so the VIEW-03/04 branches key off the design width, not the physical pixel count. `ViewMetrics` carries `px_w/px_h` (drawable), `logical_w/logical_h` (viewport), `dpi_scale`, `terminal` (the terminal rect in logical px), and `width_class`. **Terminal rect (VIEW-03 seam):** when framed (wider than the 760 breakpoint) the terminal caps at the `900×820` reference (shrinking with the window between 760 and 900), centered in the logical viewport and snapped to whole logical px so its border stays crisp; at ≤760 it is the full window at the origin (the prototype's full-width mode). **Width class:** a stacked `WidthClass { full, w760, w560, w440 }` (each "at most this width", classified by `of(logical_w)`), with `atMost(other)` as the natural stacked-breakpoint test and `framed()` gating the outer chrome — exactly the shape VIEW-04's branches consume. **Reference/breakpoints** (`ref_w=900`, `ref_h=820`, `bp_760/560/440`) are named constants. **Non-panicking:** the prologue degrades to the reference metrics on a `getSize`/`getPixelDensity` error, and `compute` floors a degenerate density to 1 and a zero size to 1. **Wired:** `Resources.view.metrics` (defaulting to reference metrics so a view built before the prologue is sane), set in `pages/root.build_ui`'s prologue beside the theme/reduced-motion projection; `View.scale = metrics.dpi_scale`. On this dev machine the density is 1.0, so `scale` stays 1 and a real SDL screenshot of the Act I HUD confirmed the layout is byte-unchanged (fills the window, text crisp) — the metrics are computed and available with no behavioral change yet; the consumers are VIEW-02 (scale all authored dims), VIEW-03 (centered terminal), and VIEW-04 (responsive branches). **Tests (SDL-free, in `view.zig`, pulled into `test-ui`):** stacked breakpoint classification and `atMost`; terminal is full 900×820 at the reference; caps+centers above the reference (1200×1000 → 900×820 at (150,90)); shrinks+centers between 760 and 900; is the full window unframed at ≤760; `dpi_scale`/drawable-px from density including the floor-to-1 and a fractional 1.5 high-DPI case. **Validation:** `zig fmt --check`, `zig ast-check`, `zig build test-ui`, `zig build test`, `zig build` all pass; LSP clean; `git diff --check` clean; production HUD screenshot verified unchanged. Docs updated in `src/ui_client/README.md` (a "Frame-local view metrics" section). `src/ui` untouched; `.kiro` untouched.
- [x] **VIEW-02 — Scale all authored dimensions consistently.** Font sizes, padding, gaps, fixed sizes, stroke widths, drag thresholds, scrollbar widths, and icon sizes consume logical units through one helper. Do not scale simulation values or camera percentages.
  - COMMENT: Established **one logical→device scaling helper**, `view.dp(logical, scale)`, and routed every authored dimension through it so the whole UI scales uniformly to device pixels — the coherent completion of the direction TEXT-04 (`type.toDevice` bakes the scale into font device px, making text boxes device px) and RENDER-06 (`paint.hairline` scales stroke widths) already set. **The model:** the layout is solved in **device px** (the space the renderer draws and text boxes already live in); every *authored* dimension is a logical value scaled once at the El/style seam — `El.with_size` (`.fixed` extents via `scaleRule`), `with_gap`, `with_offset`, `with_wrap`/`with_cell` (the text measurement width, compared against device-px font metrics), and `style.apply` (padding + gap). Fonts continue through the sibling `type.toDevice`; stroke widths through `paint.hairline` (RENDER-06); so all of them share the one `View.scale` VIEW-01 computes from DPI. The fullscreen root (`el.root`) and the modal scrim size to the drawable px (`metrics.px_w/px_h` = logical × DPI) instead of the window coordinate size, so the device-px layout fills the whole drawable. **Pointer reconciliation:** SDL delivers mouse/finger coordinates in window (logical) coordinates, but hit geometry is now device px, so every pointer coordinate is multiplied by `dpi_scale` at **one** seam in `main` (`pointerAt`, plus finger normalized→device-px) before it reaches the input model or hit-testing — keeping clicks aligned with the scaled layout. **The read-back hazard, handled:** values read back from stamped geometry (`.rect`, a parent's resolved `size.*.fixed`) are already device px; passing them through the scaling `with_size` would double-apply, so `El.with_size_px` (no scaling) was added and the handful of prior-frame-geometry sites use it — the action-tile underbar (`r.w · progress`), the ration-dial pulse (`r.w · fill`), and `play_game`'s root content box (`px_w − scaled pad`); `log_view` was made fully logical (its `line_height` measures at logical px, it receives a logical column width) so its seams scale cleanly. **Not scaled** (per the constraint): simulation values and camera percentages are not dimensions and never touch `dp`; `outline_width` is scaled at draw by `paint.hairline`, so it is *not* also scaled in `style.apply` (no double-apply). **Drag threshold:** the `>4`-px `PointerActivation`/`ThresholdDrag` boundary now compares device-px pointer deltas, so it is effectively 4 device px; the pure, tested threshold structs are left unchanged and a scale-aware threshold is noted for the board/slider consumers (INPUT-05) when they wire capture — the only authored dimension left as device-px-constant rather than routed through `dp`, because the structs are deliberately scale-unaware and no consumer exists yet. **Honest process note:** the first attempt scaled dims at the El seam without accounting for geometry read-back, which double-scaled ~5 sites; I reverted, diagnosed the logical/device-read-back boundary as the root cause, and re-applied with the `with_size_px` split and the fully-logical `log_view`, so the model is now internally consistent (verified below). **At DPI 1 (`scale == 1`) every conversion is identity**, so a real SDL screenshot of the Act I HUD is byte-unchanged from the pre-VIEW-02 baseline (same layout/spacing/borders/log footer) — the scaling is load-bearing only on a high-DPI display, which this dev machine is not, so that path is exercised by the pure `dp` unit tests rather than a screenshot. **Tests:** `view.dp` (identity at 1, ×scale, floor-at-1 for non-positive scale) added to `view.zig`'s SDL-free suite; the `style.apply` padding test now supplies a real ctx (scale 1) since `apply` reads `view.scale`. **Validation:** `zig fmt --check`, `zig ast-check`, `zig build test-ui`, `zig build test`, `zig build` all pass; LSP clean; `git diff --check` clean; production HUD screenshot verified identical. Docs updated in `src/ui_client/README.md`. `src/ui` untouched; `.kiro` untouched.
- [x] **VIEW-03 — Implement the centered terminal workspace.** At/above the reference size, cap the terminal at `900×820`, center it on `#090806`, retain its border and shadow, and clip internal surfaces. At `≤760`, switch to full-width/auto-height/full-window composition and drop the outer frame as the prototype does.
  - COMMENT: Added `pages/templates/terminal.zig` — the terminal-workspace shell every screen builds inside — consuming the VIEW-01 `metrics`. **Framed** (logical width > 760, `metrics.framed()`): `el.root` fills the window with the near-black terminal **ground** `#090806` (`rgb(9,8,6)` — darker than the terminal's own `bg` `#0e0c09`; a documented art-direction literal kept in this game-tier template, to be folded into palette tokens by KIT-01, deliberately **not** added to the generic `Theme`), and a `metrics.terminal`-sized box — capped at the `900×820` reference and centered — is placed with a soft **drop shadow** (RENDER-05 `shadow.drop`, the prototype's `0 16px 80px #000a` in device px), a hairline **border** (`line2`), its own `bg` fill, and `Layout.overflow=.clip` so screen content never spills past the edge. **Full-window** (≤760, `!framed()`): the terminal *is* the window — no ground, shadow, border, or cap — exactly the prototype's full-width mode; the returned content box is the whole window. The shell returns a `Shell { root, content }`: `root` goes to the render walk, `content` is the inner box the screen fills. **Device-px:** `metrics.terminal` is logical, so it is scaled by the frame `scale` once here (via `view.dp`), and placed with the new `El.with_offset_px` (a device-px sibling of `with_offset`, added alongside `with_size_px` so the already-device rect is not re-scaled). **Wired all three screens** off their raw `el.root` onto the shell: `play_game` (its page padding now insets the terminal content box instead of shrinking a window root; the log column width derives from `metrics.terminal.w`), `gameover`, and `act_one_end` build into `shell.content` and return `shell.root`. **Screenshots (real SDL):** at 1200×1000 the terminal caps at 900×820, centers on the darker ground with a visible border and understated shadow, and clips its internals; at 700×820 (≤760) it fills the window edge-to-edge with no frame chrome — both match the prototype. **Validation:** `zig fmt --check`, `zig ast-check`, `zig build test-ui`, `zig build test`, `zig build` all pass; LSP clean; `git diff --check` clean. Boundary: the responsive *branches inside* the terminal (stacking, column drops, padding at ≤560/≤440) are VIEW-04; this slice delivers the outer workspace + framed/full-window switch. Docs updated in `src/ui_client/README.md`; the shell is registered in the templates barrel. `src/ui` untouched; `.kiro` untouched.
- [x] **VIEW-04 — Implement explicit responsive branches.** At `≤760`, stack the page grid, release nested catalog scrolling in favor of document/page flow, stack trade columns and board/detail, and keep the log at the end. At `≤560`, use `10px` page padding, hide optional runline/subtitle copy, force two action columns, hide the BUILD effect column, and stack dialog actions. At `≤440`, use one action column and hide the BUILD cost column. Preserve control reachability and labels when columns disappear.
  - COMMENT: Established the responsive-branch mechanism — screens/templates read `metrics.width_class.atMost(.w560)` / `.w440` / `.w760` (the stacked-breakpoint test from VIEW-01) — and wired every branch that has a **real consumer today**, deferring the rest to their unbuilt templates (the same disciplined pattern as the RENDER primitives). **Wired now, in `play_game`:** page padding tightens to the prototype's `10px` at ≤560 (`16` otherwise); and the **optional** run-context label "Act I ·" is dropped at ≤560 while the **functional** Day counter always stays — reachability and labels preserved (nothing interactive is hidden; the act label is decorative context, exactly the "hide optional runline/subtitle copy" the prototype does). The ≤760 **page-grid stacking** is inherent: the HUD is already a single stacked column (header / body / footer), and the VIEW-03 terminal already switches to full-window at ≤760, so "stack the page grid + keep the log at the end" holds with no change. **Deferred with a documented seam (no consumer exists):** the two-column / one-column **action counts** (the action catalog is KIT-15/17 / ACT2-11 — today only the single tutorial `action_card` renders), **hiding the BUILD effect column (≤560) and cost column (≤440)** (the six-column RecipeRow is KIT-18; the current `build_list` is the old chip-filter version being replaced), **stacking the trade-dialog actions** (TradeDialog is KIT-21, not built), and **stacking board/detail + releasing nested catalog scrolling** (the board is BOARD-*, and CatalogViewport's ≤760 single-scroll is KIT-16). Each will branch on the same `width_class.atMost` call when it lands — building those hidden-column branches now would be inventing markup for absent templates. **Verified (real SDL screenshots):** at 540×700 (≤560) the "Act I ·" label is gone (only "Day 1" remains), the padding is visibly tighter, and the layout is the full-window unframed mode with the Forage card, stocks header, and log all reachable; at 1200×1000 the full runline ("Act I · Day 1") and 16px padding return. **Validation:** `zig fmt --check`, `zig ast-check`, `zig build test-ui`, `zig build test`, `zig build` all pass; LSP clean; `git diff --check` clean. Docs updated in `src/ui_client/README.md` (a "Responsive branches" note listing what is wired vs deferred). `src/ui` untouched; `.kiro` untouched.
- [x] **VIEW-05 — Handle resize without state loss.** Recompute layout/clip/scroll limits, clamp camera and popups, retain stable focus/selection/query/sort/collapse, and avoid a one-frame clickable ghost at obsolete geometry after a major resize.
  - COMMENT: Most of VIEW-05 is **already inherent** to the immediate-mode architecture, and I verified rather than rebuilt it: `build_ui`'s prologue recomputes `ViewMetrics` every frame (VIEW-01) and the whole tree is rebuilt from the frame arena, so **layout, clip, and scroll limits recompute for free** on any resize — a scroll view re-clamps its offset against the freshly-sized viewport each build, and the terminal re-centers/re-caps from the new `metrics.terminal`. **Focus, tab selection, query/sort/collapse, and scroll offset are retained** because they live in keyed pools addressed by the stable `node.key` (the interaction store, the focus registry, `TabsState`/`BuildViewState`/`ScrollState`/`TextInputState`), which survive the arena rebuild by key — the persistence model UIE-01/02 established and their pool tests already cover; a resize is just another rebuild with the same keys. **The one real hazard is the "one-frame clickable ghost":** the event stage routes pointer hits against the *previous* frame's stamped rects (documented frame-coupling in `ctx.zig` — `mark` runs before this frame's build/stamp), so immediately after a resize a control that moved (the centered terminal shifting, a right-anchored element) could take a click at its **old** location for one frame. Fixed in `main.zig`: a `window_resized`/`window_pixel_size_changed` event sets `App.geometry_stale` and cancels any in-flight gesture; while stale, `routePointerPress`/`routePointerRelease` suppress **activation** (a press begins nothing, a release cancels the gesture rather than emitting `.clicked`) so a click cannot land on the ghost; the flag is cleared **right after `stamp_rects`** re-stamps at the new size, so normal interaction resumes the very next frame. This is surgical — hover/wheel and the sim are unaffected, and a click during the single stale frame is dropped rather than mis-routed. **Camera clamping is deferred** with its only consumer, the STRUCTURE board (BOARD-15), which does not exist; popups re-clamp for free (the modal re-centers from `metrics` each frame, the tooltip clamps to the terminal). **Verified (real SDL):** a scripted resize sequence (500×700 → 1200×1000 → 1000×860) recomputes the layout cleanly at each step with no corruption or crash — the final frame shows the HUD correctly laid out, the responsive runline re-evaluated (the "Act I ·" label returns above 560), and sim state (Day, food) preserved across the resizes. The ghost guard itself is SDL-event glue (not unit-testable without a window); the state-retention it protects is covered by the existing keyed-pool tests. **Validation:** `zig fmt --check`, `zig ast-check`, `zig build test-ui`, `zig build test`, `zig build` all pass; LSP clean; `git diff --check` clean. Docs updated in `src/ui_client/README.md` (a "Resize without state loss" note). `src/ui` untouched; `.kiro` untouched.

## 8. Shared visual system and reusable templates — `src/pages/templates/`

### Tokens and primitive chrome

- [x] **KIT-01 — Encode the finalized design tokens once.** Use exact palette values: `bg #0e0c09`, `panel #15120f`, `line #23221e`, `line2 #3c3a33`, `dim #77746a`, `fg #b5b0a1`, `acc #a69671`, `warn #d7aa4c`, `danger #d25642`, `good #8f9b73`; resource colors Food `#d97966`, Water `#5b9fc4`, Fuel `#d5a64e`, Metal `#8497a3`, Minerals `#9a7cb4`, Biomass `#78a276`.
  - Add `good` to the shared semantic theme role set.
  - Keep six resource/sector colors in game `View`/palette data, not generic `Theme` roles.
  - Centralize body/small/heading sizes, page padding, common gaps, hairline width, control heights, and transition durations.
  - COMMENT: Tokens are now encoded once, split by **where each token belongs** rather than dumped in one file. **Colors:** the ten foundation *roles* are on `Theme` (`ui_client/theme.zig`) — I added **`good`** (success/gain, the semantic counterpart to `danger`; default a plain green so the foundation still reads with no palette) beside the existing nine, updated the module doc ("ten named roles") and the fully-defaulted test to assert it. The game's finalized *values* stay in `src/palette.zig`, where `theme.good = rgb(143,155,115)` (`#8f9b73`) joins the amber ramp; the palette values for the other nine were already the exact roadmap hex and are unchanged. Colors the engine has no concept of are deliberately **kept off `Theme`**: the terminal **ground** (`#090806`, folded out of `terminal.zig`'s file-scope literal — same value, so byte-identical render) and the six **resource/sector** hues (Food/Water/Fuel/Metal/Minerals/Biomass, exact roadmap hex) are game content in `palette.zig` (`ground`, `ResourceColors`/`resources`), now carried on `View` (`view.ground`, `view.resources`) and installed each frame in `build_ui`'s prologue — matching the sub-bullet "keep six resource/sector colors in game `View`/palette, not generic `Theme`". `terminal.zig` reads `ctx.res.view.ground` instead of its own literal. **Non-color scalars:** new leaf module `src/tokens.zig` (SDL-free, engine-free, data-only) names page padding (`pad_page 16` / `pad_page_compact 10`), a monotonic gap ladder (`gap.tight/inline_/stack/group/section/wrap`), the `hairline 1`, a control-height ladder (`control.h_compact 30`/`h_std 34`/`h_tall 40`), the KIT-05 rail widths (`rail.expanded 252`/`collapsed 36`), and **re-exports** the already-centralized typography sizes (`ui_client/type.zig`, TEXT-04) and functional-transition durations (`ui_client/tween.zig`, RENDER-08) so there's a single citation point without duplicating those numbers (guarded by "no drift" tests that assert the re-exports equal their sources). Every token is *logical* px/seconds; the one logical→device multiply stays downstream (`type.toDevice`/`view.dp`/`paint.hairline`), so a token is DPI-stable. Applied live: `play_game.zig`'s two page-padding literals and the section gap now cite `tokens.*` (VIEW-04's `10`/`16` branch), proving the centralization is consumed, not just declared. Nothing else has a token consumer yet — `good`, the resource hues, the control/rail ladders, and the extra gap steps are the vocabulary KIT-02..05 will draw from. **Verified:** `zig build test-ui`/`test`/`build` green; new `tokens.zig` tests (size/duration no-drift, monotonic ladders, rail values) and the extended `Theme` default test pass; a real SDL screenshot at the reference size is **byte-identical** to the pre-KIT-01 render (the only painted new token, `ground`, kept its value). Docs updated in `src/ui_client/README.md`. `src/ui` untouched; `.kiro` untouched.
- [x] **KIT-02 — Build primitive style/state fragments.** Cover panel, eyebrow/section heading, primary/secondary/text/icon/link buttons, tabs, chips, tags, semantic state text, row hover, focus-visible outline, disabled chrome, selected chrome, dashed provisional chrome, meter, thin progress bar, and resource legend dot.
  - COMMENT: Added the full fragment library to `ui_client/style.zig` — the shared vocabulary of *look* KIT-03..05 and every component compose from. All feed the existing `resolve`/`apply` fold (KIT-01/RENDER-02), in two flavors: **stateful** `fn(*UiCtx, *Node) Style` fragments that read the just-built node's published `Interaction` bits via `node.query(ctx)` and return the chrome for that state, and **static** `Style` values for looks with no interaction input. Delivered every item on the checklist: `panel` (fill + `line` edge), `section_heading` (the `heading` role in `acc`); the button family `btn_primary`/`btn_secondary`/`btn_text`/`btn_icon`/`btn_link` sharing one `btn_ink` rule (`dim` when `.disabled`, `acc` on `.held`/`.hovering`/`.focus_visible`, else the variant's resting ink — `btn_icon` uses `tint` for the svg cell, `btn_link` rests at `acc`); `tab` (selected → `acc` ink+border, unselected `dim`→`fg` on hover), `chip` (filled pill, accents when selected/hovered), `tag` (static `line`-bordered `dim` label); semantic state text `state_good`/`state_warn`/`state_danger`/`state_muted` projecting the KIT-01 roles (including the new `good`) to ink; interaction chrome `row_hover` (lifts fill to `panel` on hover/focus, transparent at rest), `focus_ring` (an `acc` ring **only** on `.focus_visible`, never bare hover — the accessibility contract), `disabled_chrome` (forces `dim`, composed last so a disabled control can't read active), `selected_chrome` (`acc` border+ink on `.selected`), `provisional` (the `dim` dashed placeholder box); and the small chrome `meter_track`/`meter_fill` (+ `progress_fill` alias — "thin" is the node's height, set imperatively), and `legend_dot(comptime Resource)` filling with the named hue from `view.resources` (the six KIT-01 resource colors), so a legend cites the resource not a color. **Every color is a `Theme` role, never a literal**, which is why the library lives in the foundation (`style.zig`): it encodes role/interaction *policy*, not the game's values; precedence stays the caller's (a later tuple fragment overrides one variant field). No call site consumes them yet — this is the vocabulary; KIT-03 wires it into the button box and KIT-04/05 into the shell/rail — so there is **no visual change** and no screenshot is warranted. Added `theme` import to `style.zig` for the shared theme type. **Verified:** 9 new unit tests (a `KitFixture` installs a known theme+resources on a real `UiCtx`, sets interaction flags via `ctx.setFlag`, and asserts each fragment's resolved fields across states — btn_primary state ladder incl. disabled-wins-over-hover, tab selected/unselected, the four semantic inks, focus_ring only-on-focus-visible, row_hover rest-vs-hover, provisional dashed, legend_dot hue, and last-wins precedence); `zig build test-ui`/`test`/`build` all green. Docs updated in `src/ui_client/README.md` (Style section). `src/ui` untouched; `.kiro` untouched.
- [x] **KIT-03 — Make button semantics consistent.** The whole outer box owns interaction; children remain content. Disabled controls cannot focus/activate, pressed state is visible, Enter/Space matches pointer activation, and nested cancel/close actions consume activation without losing ancestor hover.
  - COMMENT: Made the stock buttons (`widgets.button`, `icon_button`) follow one contract, mostly by *tightening* the widget onto mechanisms the engine already had and *single-sourcing* their color. **Whole box owns interaction / children are content:** unchanged and confirmed — the outer node is the queried/focusable/stamped surface; the label/sprite is a child with no interaction. **Disabled cannot focus/activate:** `registerFocus(key, enabled)` already kept a disabled control out of the focus ring (verified by `focus.zig`'s "global traversal skips disabled targets" test, so keyboard Enter/Space can never reach it); I added the missing half — when a pointer `.clicked` lands on a disabled button, the widget now **consumes** it (`ctx.consumeFlag(key, .clicked)`), which clears the flag on the node *and* its ancestors, so a disabled button neither reports `.clicked` to its caller nor lets the click bubble to an ancestor control. (Previously the widget relied on the caller's domain gate; now activation is impossible at the source.) **Pressed visible:** the `.held` bit lifts the ink to `acc`, so a press reads distinctly — preserved, now via the shared fragment. **Enter/Space matches pointer:** verified the path already exists and needs nothing new — `command.zig` maps `return_key`/`return_key2`/`kp_enter`/`space` → `.activate`, and `main.routeCommand(.activate)` does `markKey(focusedKey, .clicked)`, the *same* flag a pointer release emits (`PointerActivation.release` → `markKey(.clicked)`), so keyboard and pointer activation are the one shared flag. **Nested cancel/close consumes without losing ancestor hover:** the mechanism is `El.consume(.clicked)` → `consumeFlag`, which clears `.clicked` on the node and every ancestor (so a row's cancel `×` activates without the row also acting) while leaving `hovering` alone (only `.clicked` is consumed) — exercised at the engine by ctx.zig's "captured child activation can be consumed" and "mark bubbles to ancestors, so a row stays hovered over its own button"; `capital_row.zig` already uses this for its cancel `×`. **Consistency win:** both buttons now resolve the KIT-02 `style.btn_primary`/`btn_icon` fragment and paint border + ink from that single result, replacing the duplicated inline `if (!enabled) dim else if (held|hover|focus) acc else fg` expressions — one state→role mapping for every button, so variants can't drift. The resolved colors are identical to the prior inline logic (publish sets `focus_visible = focused`, matching the fragment), so the render is unchanged. **Verified:** new ctx_binding test "a disabled control consumes its click so it neither reports nor bubbles"; `zig build test-ui`/`test`/`build` green; a real SDL screenshot of the Forage button is **byte-identical** to the pre-KIT-03 render. Docs updated in `src/ui_client/README.md` (Pointer activation section). `src/ui` untouched; `.kiro` untouched.

### Shared shell

- [x] **KIT-04 — Build one terminal shell template.** Compose header, flexible page grid, side rail, main surface, navigation, optional market/activity strips, view body, footer log, and overlay roots. The shell accepts act identity and view descriptors rather than duplicating Act I/II markup.
  - COMMENT: Extended `pages/templates/terminal.zig` (the VIEW-03 outer chrome) with `shell(ctx, opts) -> Regions` — the **one region skeleton every screen composes into**, driven by a descriptor instead of hand-built markup. A screen passes `ShellOptions { id, act, page_pad, section_gap, rail, nav, market, activity, footer }` and receives a `Regions` struct of `El` handles: always-present `header` (thin top strip) and `body` (the main surface that `grow`s to fill), plus the optional `rail`, `nav`, `market`, `activity`, and bottom-anchored `footer`, each `null` unless the descriptor turned it on (`if (regions.rail) |rail| …`). The interior is laid out once in the prototype's arrangement — content box (page padding, column, section gap) → `header` → optional `market`/`activity` strips → a **page-grid** row (`grow`s to fill height) holding `rail | body` → optional `nav` → bottom-anchored `footer` — built on top of `terminal` so the framed/full-window chrome, cap, shadow, border, and clip are unchanged; `shell` only lays out the interior. **`act` identity + descriptors, not duplicated markup:** the shell carries the `Act { act_one, act_two }` enum and the region toggles, so the same shell serves both acts — Act I turns on `footer`, Act II will turn on `rail`/`nav`/strips against the identical skeleton — rather than each act re-declaring the region graph. **Overlay roots** are deliberately kept as the screen's own top-layer roots (tooltip/modal are stamped after the shell, outside the terminal box) so they float above the clip and can cover any control; the shell owns only the in-terminal regions, keeping the clip honest. **Migrated all three screens:** `play_game` now requests `{ .footer = true }` and fills `header`/`body`/`footer` (dropping ~30 lines of hand-built header/column/footer wiring); `gameover` and `act_one_end` request just the body and fill `regions.body` with their centered content. The rail/nav/market/activity regions are the vocabulary KIT-05 (rail) and Act II (nav/strips) consume; they have no filler yet. **Verified (real SDL):** the play HUD lays out correctly — header strip (V/F/M · Act I·Day), the Forage teaching card centered in the growing body region, the event-log footer bottom-anchored full width; no clipping/corruption. The card sits slightly lower than before because it now centers in the `body` region *between* the reserved header and footer rather than in the whole content box (the footer's band is no longer overlapped) — a structural correctness improvement, not a regression. `zig build test-ui`/`test`/`build` all green; region composition is layout glue best verified visually (the VIEW-03 pattern), so no isolated unit test. Docs updated in `src/ui_client/README.md`. `src/ui` untouched; `.kiro` untouched.
- [x] **KIT-05 — Build the collapsible Holdings/BODY rail.** Expanded width is `252`, collapsed width `36`, with a `120ms` transition and vertical restore affordance. Maintain separate ACTIONS/BUILD memories; force STRUCTURE collapsed on every entry without erasing those values. Expanded/collapsed state, labels, focus, and hit targets remain synchronized.
  - COMMENT: Added `pages/templates/rail.zig` — the collapsible side rail that fills the KIT-04 shell's `regions.rail`, wiring together the pieces the earlier slices built. **Widths are the KIT-01 tokens:** expanded `tokens.rail.expanded` (252), collapsed `tokens.rail.collapsed` (36); no bare literals. **The 120ms transition is the RENDER-08 tween:** the rail keys a scalar tween by its own `node.key`, `retarget`s it toward the target width over `tokens.dur_rail_s` (120ms) each frame, and drives the rail box's fixed width from `value(key, target)` — so a toggle animates, a mid-transition re-toggle reverses smoothly from the current width (the registry's interrupt/reverse), a first appearance sits at its target, and reduced motion snaps (the registry's policy, projected each frame in `main`). **The vertical restore affordance is TEXT-06:** collapsed, the strip shows a chevron above the rail's label set 90° counter-clockwise via `El.vertical()` (reading bottom-to-top), at the prototype's 9px/0.09em collapsed-rail typography. **Separate ACTIONS/BUILD memories + STRUCTURE forced collapsed:** the collapse bit lives in a new keyed `RailState { collapsed }` pool (added to `UiState`) addressed by the rail's `node.key` — distinct rails carry distinct ids, so the ACTIONS and BUILD rails keep independent collapse memory *for free*; the STRUCTURE rail passes `force_collapsed`, which overwrites only *its own* key's bit to collapsed on every entry, leaving the other keys' remembered values untouched (a unit test models exactly this: forcing one rail's bit does not change the others'). **State/labels/focus/hit-targets stay synchronized** because the whole strip owns the toggle (the KIT-03 button contract — outer box owns interaction, a click flips the one bit) and the affordance/label/body are rebuilt from that single bit each frame, so they can never disagree; the rotated node's box and hit target are the swapped (tall) footprint via TEXT-06's axis swap. **Wired into `play_game`:** turned on `regions.rail` in the shell descriptor and moved **Holdings** out of the cramped 640-wide centre column (its documented mis-placement) into the rail, where the prototype puts it; `holdings.zig` now sizes to its parent width (the rail body, a definite width) instead of a fixed 300, so its right-aligned rows track the rail. Removed the TEXT-06 collapsed-rail stand-in fixture from `mock.zig` now that the real rail supersedes it. **Verified (real SDL):** with the run underway the expanded rail renders correctly to the left of the body — the "HOLDINGS" eyebrow header with a collapse chevron over the Holdings panel, the ACTIONS/BUILD tabs + tiles + ration dial in the body, the footer log across the bottom — no clipping/overlap. (The click-to-advance was seeded by temporarily defaulting `tutorial_done` for the capture, then reverted — confirmed `git diff` shows no `tutorial_done` change.) 4 new SDL-free tests: target width is the token (never a literal); the width rides the tween between 252 and 36 and lands on target; reduced motion snaps; distinct rails keep separate collapse memory while `force_collapsed` overrides only its key. `zig build test-ui`/`test`/`build` all green. Docs updated in `src/ui_client/README.md` (tween section). `src/ui` untouched; `.kiro` untouched.
- [x] **KIT-06 — Rebuild Holdings as a live reusable panel.** Show header tail `{kinds} kinds, {units}`, owned rows/counts, act-specific MARGINS, positive/danger semantic values, and BODY projections. Derive all values from domain state/catalog baselines; no copied tuning constants.
  - COMMENT: Brought `pages/templates/holdings.zig` up to the full KIT-06 contract; it was already a reusable panel deriving margins from catalog defaults (not copied tuning), so this slice completed the missing pieces and made the readouts *semantic*. **Header tail** `HOLDINGS {kinds} kinds, {units}` — kinds from `capital.goods_owned`, units summed from the owned components' `count` (unchanged, confirmed). **Owned rows/counts** — one line per owned kind with `x{count}` only when there are spares (unchanged). **Act-specific MARGINS with positive/danger semantic values** — `margin_line` compares each live action's `requires.energy` against its own catalog default `(ActionT{}).requires.energy` and now colors the delta by the new KIT-01 roles: a *lower* live cost is an improvement (`good`), a *higher* one a penalty (`danger`) — so a margin reads as good/bad, not a flat accent. The generator food inflow is a positive flow → `good`. **BODY projection** — a new block projecting how long the larder lasts at the current ration: `net = base_rate × ration_mult(setting) − generator_inflow`, days `= food / net`; `≤0` shows "Larder holds" (`good`), otherwise "Food lasts {d} days" colored `danger` under ~2 days. **No copied tuning constant:** the projection calls the very function the metabolism loop applies — I made `systems.ration_mult` public and added `systems.net_food_drawdown` beside it, so Holdings reads the loop's `base_rate × mult` product rather than a local copy; a drift is impossible by construction. **Reusable across acts:** the panel takes `(parent, world, entity, id)` and sizes to its parent width (KIT-05 rail body), so Act II's BODY rail reuses it unchanged; the margin rows are act-specific by which action components the entity holds. **Verified:** the empty state ("0 kinds / nothing built yet") renders correctly in the rail (real SDL); the margin/BODY blocks are gated behind `kinds > 0` (nothing to show for a bare actor), and their *math* is covered by new SDL-free tests in `systems.zig` — `ration_mult` returns the config scales, and `net_food_drawdown` is consumption−inflow with days = food/net and the ration/normal/feast multipliers matching the loop — placed in `systems.zig` (not the page template) so `zig build test` actually discovers them. `zig build test-ui`/`test`/`build` all green. `src/ui` untouched; `.kiro` untouched.
- [x] **KIT-07 — Build shared tab/view navigation.** Exactly one tab is selected and globally focusable; arrows rove within the tablist; Enter/Space switches; inactive panels are not built/focusable but their state persists in the shell. Views restore their own scroll/collapse state.
  - COMMENT: Added `pages/templates/view_nav.zig` — the shared tab/view navigation, a thin reusable layer over the existing `tabs` strip that formalizes the view-switching contract so no screen re-implements it. The underlying mechanisms already existed and are reused unchanged: **exactly one selected** (the strip's pooled `TabsState`, one active index), **globally focusable as one Tab stop** (roving focus via `registerRovingFocus` — the tablist contributes a single global stop, so Tab reaches "the tabs" once), **arrows rove within** (`moveFocusedRoving` on `.move_*`), and **Enter/Space switches** (`command.activate` → `.clicked` on the focused chip → sets the index the same frame — the KIT-03 shared activation flag). What KIT-07 adds is the **view-retention contract in one place**: `view_nav(ctx, nav_parent, id, labels) -> { active, el }` renders the tablist (into any parent — the shell's `regions.nav`, or a screen's own column), and `retain_view(ctx, parent, child_id, T)` keeps an **inactive** view's pooled state alive by key without building its nodes, so an unbuilt view is neither focusable nor drawn yet its scroll/collapse/query/sort survives the frame-arena rebuild and restores exactly on return (a no-name wrapper over `Node.retainChildState`, so a screen retains a view without naming the engine primitive). **Wired into `play_game`:** the ACTIONS/BUILD switch now goes through `view_nav`, and the inactive BUILD view's `BuildViewState` (sort/filter) is kept via `retain_view` — replacing the raw `tabs` + inline `retainChildState` with the shared component, behavior-identical. The shell's `regions.nav` is available for Act II's different layout; Act I keeps its centered "classic tab silhouette" by passing its own center column as the nav parent. **Verified:** behavior-preserving (the tablist chrome, roving focus, selection persistence, and a11y group all come from `tabs` unchanged, so the render is identical — the ACTIONS/BUILD strip shown in the KIT-06 capture is the same); `zig build test-ui`/`test`/`build` all green. `src/ui` untouched; `.kiro` untouched.

### Overlay and popup controls

- [x] **KIT-08 — Rebuild modal, tooltip, and text input on `El`.** Modal provides backdrop, centered shell, focus trap, initial focus, Escape/close/Leave dismissal, optional outside-click policy, and restoration to the opener. Tooltip is clamped to the terminal and never intercepts input. Text input uses the completed editing foundation.
  - COMMENT: The three overlay controls were already built on `El`/`Node` (backdrop + centered box + Escape target + outside-click via `consumeFlag`; tooltip its own root; `text_input` on the `editor.zig` `LineEditor` model), so this slice added the genuinely-missing pieces. **Modal focus trap:** added a bounded focus **scope** to the engine (`ui/focus.zig`) — `openScope()` records the index in the build order from which the trapped set begins, carried into the next frame's `current` so `move` (Tab/Shift+Tab) traverses only that suffix; the `global*` traversal helpers gained a `respect` flag so the `endFrame` repair (which walks `building`) is *not* scope-filtered, only `move` is. `Ctx.beginFocusScope()` exposes it, and `modal` calls it right after the dialog box is built — since a modal is stamped last, its focusables are exactly the trapped suffix, so Tab can never reach a scrim-covered control. **Initial focus + restore-to-opener** are documented as caller policy (only the caller knows the open/close transition in immediate mode: request focus on the primary control the frame it opens; record `focusedKey()` before opening and re-request it the frame it closes) — the trap is the engine half, the transition is the screen half. **Escape/close/Leave dismissal + outside-click** already worked (root registered as the Escape target receiving `.dismissed`; `consumeFlag(box, .clicked)` then a `root.clicked` read is an outside activation) — unchanged. **Tooltip now never intercepts input:** marked `setPassThrough`, so despite being stamped last it takes no pointer hit and does not occlude the control it describes; added `clampToTerminal(x,y,w,h)` to keep it inside the terminal rect (device px, scaled from `metrics.terminal`). **Text input** confirmed on the completed editing foundation (`UiState.TextInputState = editor.LineEditor`) — unchanged. **No production consumer yet** (no modal/tooltip is built in the current Act I screens — the curtain is a screen swap, not an overlay), so there is **no visual change** and no screenshot; the new behavior is covered by unit tests. **Verified:** 2 new `focus.zig` tests — an open scope confines Tab traversal to the modal's suffix (and wraps within it), and the trap lifts when the modal closes (whole-frame traversal returns, reaching the background); the existing focus/roving/repair tests still pass unchanged, confirming the `respect`-flag split did not regress ordinary traversal. `zig build test-ui`/`test`/`build` all green. Docs updated in `src/ui_client/README.md` (overlay section). `.kiro` untouched.
- [x] **KIT-09 — Build a keyboard-operable select/popup control.** It supports label/value, open/close, arrows, Enter, Escape, disabled state, focus restoration, top-layer clipping, and compact `30px` logical height. Use it for sort kind/direction rather than recreating one per catalog.
  - COMMENT: Added `pages/templates/select.zig` — one reusable `label value ▾` select with a popup list, plus a **pure state machine** so the pointer path and any keyboard routing drive one source of truth. The machine lives as methods on a new pooled `UiState.SelectState { open, value, highlight }` (keyed by the control node, survives the frame rebuild): `openPopup`/`toggle` (seed the highlight at the committed value; disabled is inert), `cancel` (Escape/outside — close without committing), `moveHighlight(delta, n)` (arrows; clamped at the ends, no wrap — it is a list, not a ring; no-op while closed), `commit(index, n)`/`commitHighlight(n)` (Enter or a click — set value, clamp out-of-range, close). Checklist: **label/value** — the closed control renders `{label} {value} ▾`; **open/close** — clicking toggles, clicking an option commits+closes, Escape/outside cancels; **arrows/Enter/Escape** — encoded in the machine (a host routes key events into it exactly as `main.zig` routes text into the editor); **disabled** — cannot open or commit (KIT-03 contract: a stray `.clicked` is consumed); **focus restoration** — the control requests focus on click and owns it, so closing returns keyboard flow to the select, not the vanished popup; **top-layer clipping** — the popup is an absolutely-offset overlay child with `.clip`, drawn after the closed control so it floats over following siblings and clips its own contents; **compact height** — the control stands at `tokens.control.h_compact` (30 logical px), and the border/caret follow focus/hover via the theme roles. The pointer path (hover moves the highlight, click commits) drives the same `SelectState` methods, so pointer and keyboard cannot diverge. **No production consumer yet** — the sort kind/direction selects are KIT-15; this is the reusable control they will call — so there is **no visual change** and no screenshot. **Verified:** the pure state machine is unit-tested (toggle seeds highlight / disabled inert / arrows clamp-no-wrap-only-while-open / Enter commits+closes / Escape closes without committing / commit clamps out-of-range) in `ctx_binding.zig` (covered by `test-ui`, unlike a page template); LSP-clean; `zig build test-ui`/`test`/`build` all green. `src/ui` untouched; `.kiro` untouched.
- [x] **KIT-10 — Build a range slider.** Support pointer capture, arrows/PageUp/PageDown/Home/End, min/max/step, diamond thumb, split gradient track, focus ring, accessible value text, and live updates. The control owns no eating math.
  - COMMENT: Added `pages/templates/slider.zig` — a horizontal range slider whose **value math is pure and lives on `UiState.SliderModel`** (min/max/step) so the pointer drag and the keyboard both resolve through one grid: `snap` (clamp to range + snap to the nearest step from min; step 0 = continuous), `fromFraction`/`toFraction` (track position ↔ value, for the pointer and the thumb), and `nudge(v, key)` for the keyboard — arrows ±one step, PageUp/PageDown ±a coarse step (`max(step×10, range/10)`), Home/End jump to the ends, all snapped and clamped. **The control owns no domain math** ("no eating math"): `slider(ctx, parent, id, label, value, min, max, step, enabled) -> f32` takes the caller's current value and returns the new one — the caller maps it to a rate (KIT-19's `0.5×..2.0×` mapping is the caller's, not the slider's) — so updates are **live** and the value stays the caller's source of truth; only the transient **pointer capture** lives in a pooled `SliderState { dragging, pointer_kind, pointer_id }` keyed by the node. Anatomy per the checklist: **pointer capture** on the track (press captures + jumps to the pointer fraction, drag maps pointer x → `fromFraction`, release frees capture — the `updateScrollThumb` capture/identity/cancel pattern); **split gradient track** via the RENDER-04 gradient feature (a hard `acc→line` split at the value fraction — two coincident stops); **diamond thumb** centered on the fraction over the track; **focus ring** via the KIT-02 `focus_ring` (only on `focus_visible`); **accessible value text** rendered beside the control; **compact** at `tokens.control.h_compact`. Arrows/Page/Home/End are the `SliderModel.nudge` transitions a host routes key events into (the documented contract, same as the editor/select). **No production consumer yet** — EatingPolicy/BODY is KIT-19; this is the reusable control it will call (and the existing `ration_dial` chips stay until then) — so there is **no visual change** and no screenshot; the render composes already-verified features (RENDER-04 gradient, KIT-02 focus ring). **Verified:** the pure value math is unit-tested in `ctx_binding.zig` (covered by `test-ui`) — `snap` clamps + snaps to the step grid (incl. coarse step and continuous), fraction round-trips ends/midpoint both directions, and `nudge` gives ±step for arrows, the coarse page step, Home/End jumps, and end-clamping; the template is LSP-clean; `zig build test-ui`/`test`/`build` all green. `src/ui` untouched; `.kiro` untouched.
- [x] **KIT-11 — Build a disclosure control.** Synchronize expanded state, details visibility, `details ↓`/`close ↑`, focus, and compact summary. Use it for milestone details and sort panels where anatomy permits.
  - COMMENT: Added `pages/templates/disclosure.zig` — a summary row toggling a disclosed details section, synchronized by **one bit**. The expanded/collapsed state is a pooled `UiState.DisclosureState { expanded }` keyed by the control node (survives the frame-arena rebuild). The **whole summary row owns the toggle** (KIT-03 button contract: outer box owns interaction, a click flips the bit; a disabled control consumes the stray `.clicked`), and everything else is rebuilt from that one bit each frame so they cannot disagree: the **affordance label** reads `details ↓` when collapsed and `close ↑` when expanded (accenting on hover/focus); **details visibility** — the details subtree is built *only while expanded*, so a collapsed disclosure has no details nodes and nothing in them is focusable; **focus** — the row requests/owns focus on click; the **compact summary** is always shown. `disclosure(ctx, parent, id, summary, enabled) -> { expanded, details: ?El }` returns the details box to fill when open. **Reusable** for KIT-20 milestone details and the KIT-15 sort panel (the two "summary over disclosed body" anatomies the roadmap names). **No production consumer yet** (milestones are KIT-20; KIT-15 wires the sort panel next), so **no visual change** and no screenshot; the toggle mechanism is the KIT-03 flip already covered by the "disabled consumes its click" and bubble/consume engine tests, and the state is a single bool. **Verified:** LSP-clean; `zig build test-ui`/`test`/`build` all green. `src/ui` untouched; `.kiro` untouched.

### Shared status and catalog components

- [x] **KIT-12 — Build StockToken and Runline.** Stock tokens render two-letter abbreviations, full labels on hover/focus, low/zero danger values, separators, and stable alignment while label width changes. Preserve Act I's direct abbreviation/full-label swap and Act II's compact-token font expansion (roughly `10→17px` in the current CSS) without shifting neighboring controls outside the stockline. Runline renders generated/consumed energy rate, act label, and day. Token order and membership come from the act descriptor.
  - COMMENT: Added `pages/templates/stock_token.zig` (`stock_token` + `stockline` + the `Stock` descriptor) and `pages/templates/runline.zig`, and wired `play_game` onto them (retiring the bespoke `resource_bar`/inline-runline). **StockToken:** each token shows a **two-letter abbreviation** with its value (`Vi 10/10`) and swaps to the **full label** on hover *or* focus (`Vigor 10/10`) — it registers focus so keyboard reveal works too. The crux, **stable alignment while the label width changes**, is solved with TEXT-03: the whole token renders inside a fixed-width **cell** (`El.with_cell(.clip, cell_w)`), so its layout/hit box is always `cell_w` regardless of the short/long form — a widening label clips within its own cell and **never shifts the neighboring tokens** (Act I's direct abbrev/full swap; Act II's `10→17px` compact-token font expansion rides the same cell unchanged). A **low/zero** value tints the whole token `danger`. Tokens are `|`-separated by dim dividers, baseline-aligned like the old chips. **Token order and membership come from the act descriptor:** `stockline(ctx, parent, id, stocks: []const Stock)` takes the caller's slice, so Act I lists Vigor/Food/Materials and Act II will list its sectors without the template hard-coding either; the values are formatted by the caller (the token owns no domain math), and `danger` is the caller's threshold (vigor SPENT, food ≤0). **Runline:** `runline(ctx, parent, id, Run{ gen, consumed, act_label, day, compact })` renders the energy **rate** (`+gen −consumed`, omitted when both are 0 — Act I has no stored energy flow, so only act/day show), the **act label** (dropped when `compact`, VIEW-04 ≤560), and the **day** (the functional counter, always in `fg`). **Verified (real SDL):** the header renders `Vi 10/10 | Fo 4 | Ma 0` in stable fixed-width cells with `|` separators, and `Act I · Day N` on the right — clean, no neighbor shift; matches the prior header's information with the reusable tokens. `zig build test-ui`/`test`/`build` all green. `src/ui` untouched; `.kiro` untouched. (`resource_bar` is left exported but is now unused in production — a later cleanup slice can drop it.)
- [ ] **KIT-13 — Build MarketStrip and ActivityStrip.** Identity map selects `● PASSERBY`, `● MERCHANT`, or `◆ EXCHANGE`, copy, action label, and semantic color. Activity supports idle/working/building glyph, subject, metadata, live announcement, and exact dim/accent/warn styling. STRUCTURE can suppress both without losing state.
- [ ] **KIT-14 — Extract one catalog query parser.** Parse whitespace terms with quotes, leading negation, `*`, `field:value`, comma-OR values, and case-insensitive matching without allocating per row. Parameterize allowed fields for actions (`in/input`, `out/output`, `type/kind`, `state/status`, `is`) and recipes (the same plus `tech`); `built` aliases `owned`, `state:in-reach` means ready or reach≥`0.5`, and `is` matches either state or type. Free text searches the complete presentation haystack. Unit-test malformed quotes, empty values, aliases, negation, phrases, wildcard, and combined fields.
- [ ] **KIT-15 — Build CatalogControls.** Search field, `/` shortcut only for the active catalog view, Escape-clear, adjacent sort toggle, disclosed Sort-by and Direction selects, `none` direction disabling/reset, count-only live summary, deterministic name tie-break, and reset-to-top on query/sort change. Escape inside the sort row closes it and restores focus to its toggle. ACTIONS and BUILD keep independent query/sort/open/scroll state. Default filtering remains supplied by each surface—ACTIONS shows its entire unlocked roster, while BUILD applies its in-reach/unowned default.
- [ ] **KIT-16 — Build one CatalogViewport.** Keep controls and Eating Policy/milestone/footer fixed while only results scroll at desktop sizes; reserve an `8px` logical scrollbar gutter with the prototype's inset thumb treatment; clamp/persist wheel and draggable-thumb state; reset on requested data changes; remove nested result scrolling at `≤760` and use one shell/page scroll region instead. BUILD's heading stays visually fixed by placing it outside the moving result content rather than requiring generic sticky positioning. Opening Exchange details consumes result-viewport height instead of increasing the terminal's total height.
- [ ] **KIT-17 — Normalize ActionTile.** Render name, optional state line only when nonempty, metrics `−{energy}e {optional additional input costs} · {duration}`, distribution glyph with the shared normal/Poisson/uniform/exponential/fixed accessible labels, and expected yield. Support ready, unavailable/short, and working/progress states; additional costs such as Check traps' `−1m` remain in the price segment before the middot. The same renderer handles Act I and Act II records; catalogs decide density and columns.
- [ ] **KIT-18 — Build RecipeRow/Table.** Data schema is name/type, cost, time, effect, state, action. Render name with secondary type, synchronized six columns, 3-segment reach meter, live progress/time, `Build →`, `Build +1`, and cancel `×`. A building row is pinned first and remains visible through every query/sort. Hidden columns follow width classes.
- [ ] **KIT-19 — Build EatingPolicy/BODY.** Reuse a `0..100`, step-`1` range slider and map it exactly with `rate = 0.5 × 4^(value/100)` (`0.5×..2.0×`, `1.0×` at `50`). Word thresholds are `≤12 meager`, `≤28 lean`, `≤44 modest`, `≤58 normal`, `≤72 hearty`, `≤88 generous`, otherwise lavish. Render `{word} · {rate:.2}×`, aria/value narration `{word}, {rate:.2} times normal`, `coverage = baseCoverage/rate`, and `recovery = max(0.1, baseRecovery/rate)` rounded to one decimal day. Act I config is `5.2/0.224`; Act II is `10.3/0.448`. Keep the display/math implementation shared while the authoritative simulation stores the resulting bounded multiplier.
- [ ] **KIT-20 — Build MilestoneGoal.** Shared anatomy: fixed left semantic bar/wash, disclosure, title, readiness, compact summary, primary action, details kicker/cost/copy, requirement row, and explanation. Shared lifecycle is locked → unfunded → ready → done; Shelter and Exchange supply wording, requirements, and terminal state copy. The component stays outside recipe filtering/sorting; disclosure uses allocated viewport space rather than growing the fixed terminal.
- [ ] **KIT-21 — Build TradeDialog.** Shared header/tabs/three-column body/footer. Identity map supplies kicker/title, column headings, buy/sell give label, CTA, footer note, and venue identity. Offer list has exclusive selection, finite stock, disabled/refusal copy, preview give/receive/effect, live holdings, and atomic completion result handling. Mode change selects the first valid offer without discarding per-mode selection unnecessarily.
- [ ] **KIT-22 — Normalize EventLog.** Four visible newest-first lines, newest emphasized, tone-to-theme mapping including `good`, timestamps/`NOW`, polite live announcements, shared ID/contract at screen level, preserved history beyond four in `Log`, and no independent scrolling unless history view is explicitly opened.
- [ ] **KIT-23 — Build board-level Tag, CostList, Legend, ZoomControls, and EmptyState primitives.** Keep these generic game templates; technology/domain wording stays in the STRUCTURE screen.

## 9. Act I authoritative backend and screen

### Domain contracts

- [ ] **ACT1-01 — Put starting baselines in authoritative component/catalog defaults.** Move Vigor ceiling, Food quality, and spoilage baselines out of `spawn_agent` literals so Holdings can derive current-vs-base margins. Spawn from those defaults. Test that a new player exactly matches catalog baseline.
- [ ] **ACT1-02 — Replace the three-value metabolism enum with a bounded scalar.** `comp.Metabolism.Setting` currently exposes only ration/normal/feast. Replace it with a persisted agent-local eating multiplier in `[0.5, 2.0]`, normal at `1.0`. `systems.metabolize` uses it continuously; config defines range/default, not UI words. Migrate/reset old state deterministically.
- [ ] **ACT1-03 — Add authoritative BODY projections.** Given current Food, Food quality/spoilage, Vigor deficit/ceiling, base metabolism, vigor-per-food, and selected rate, return food coverage and time to full vigor or explicit unavailable/no-recovery states. Unit-test empty food, full vigor, zero quality, min/normal/max rate, and rounding boundaries.
- [ ] **ACT1-04 — Use an absolute Shelter Vigor requirement.** The finalized requirement is current Vigor `15`, not a percentage of an upgradeable ceiling. Extend catalog unlock data and use the same result for checklist, action enablement, and `begin_build`. Test `14.99` fails and `15` passes regardless of max Vigor.
- [ ] **ACT1-05 — Define a stable action/recipe presentation catalog.** Map current typed action/good components to stable IDs, display name, type, input/output keys, effect copy, technology/prerequisite eligibility, and authored order. Reconcile existing `ActionHunt`/16 goods with the finalized Act I surface explicitly; do not accidentally show extra entries because a component exists.
- [ ] **ACT1-06 — Add eligibility transition tracking.** Compare the current eligible recipe/action set with prior authoritative/session state, emit one “A new recipe is within reach: {name}.” log when a prerequisite becomes satisfied, and avoid repeats on tab switches, filter changes, load, or sort.

### Act I shell and surfaces

- [ ] **ACT1-07 — Replace the current `play_game.zig` composition with the shared shell.** Match the finalized `900×820` Act I layout, VI/FO/MA header, energy/day runline, persistent rail, ACTIONS/BUILD tabs, Passerby strip, activity strip, and four-line footer. Remove the tutorial-only alternate layout from the production parity path or redesign it as a deliberate pre-prototype state that transitions into the same shell without layout drift.
- [ ] **ACT1-08 — Implement finalized ACTIONS.** Render exactly the canonical fixture roster—Forage (`−1.7e · 4h`, normal, `+1–3f`), Scavenge (`−2e · 5h`, exponential, `+0–7m`), Split wood (`−2.5e · 5h`, uniform, `+3–5m`), Fish (`−2e · 6h`, Poisson, `+2–5f`), Check traps (`−1e −1m · 2h`, Poisson, `+1–4f`)—plus Eating Policy in a two-column grid (one column at `≤440`). Values derive from live records/quotes; the fixture verifies presentation, not duplicated constants. Activation begins labor once, updates Activity, and logs the upfront payment copy. All-ready tiles omit the optional state line.
- [ ] **ACT1-09 — Implement finalized BUILD over live catalog data.** Replace SORT/SHOW/TIER/built chips and old `Kind.blocked` with CatalogControls and RecipeRow. Default sort is reach descending. Blank query shows ready plus reach≥`0.5`, excluding owned; any nonblank query searches all non-locked records, including owned and more distant reach states. Prerequisite-locked rows remain absent, while a building row is always visible/pinned. The canonical 8-record fixture renders `4 of 8 recipes shown`; the total is the full authored catalog count even though locked rows have no result row. Remove the stale initial `4 of 6` copy. Summary always uses `{visible} of {authored total} recipes shown`.
- [ ] **ACT1-10 — Preserve the real build lifecycle.** Starting a ready/owned recipe pays through `capital.begin_build`, pins the live `Busy` row, disables other starts, scrolls to top, and shows progress/time/cancel. Cancel calls the existing refund path, restores row state, updates Activity, and logs once. Completion increments/owns through the simulation and removes progress without UI-local timers.
- [ ] **ACT1-11 — Implement live Shelter milestone.** Instantiate `MilestoneGoal` outside filtering. Canonical requirements are current Vigor `15`, Food `12`, eligible/built recipe kinds `5`, and Materials `60`; build price/copy shows `60m 6e · 6.0d`. Recompute every check from authoritative state, distinguish locked/unfunded/ready, disclose details, begin the real Shelter build, and transition only after completion. Keep consumed values/history meaningful after payment.

### Passerby and barter

- [ ] **ACT1-12 — Add merchant encounter state.** Model absent, approaching, present, and departed; stable encounter identity; arrival/departure events; remaining time; finite inventory; and availability independent of whichever tab is visible. The UI reads state and does not infer it from a hardcoded day.
- [ ] **ACT1-13 — Define shared typed bundle and quote contracts.** Each offer has stable ID, direction, give/receive bundles, effect preview, stock, affordability/refusal reason, expiry, revision, and next marginal sell quote. Food, Materials, and owned goods use one transfer representation that can extend to Act II typed resources/Coin.
- [ ] **ACT1-14 — Resolve barter atomically.** On acceptance, revalidate encounter/offer revision, affordability, stock, and ownership; transfer both bundles or neither; decrement finite stock; apply/revoke/supersede effects once; and emit one result/log. Return player-readable stale, departed, sold-out, and insufficient-stock reasons. Unit-test every refusal and rollback path.
- [ ] **ACT1-15 — Implement diminishing marginal sell prices.** Author finite value schedules or a clear valuation rule so repeated units receive lower offers, expose the next quote before confirmation, and test monotonic decline without making all production-for-sale strictly dominated. These are the first explicit exchange numbers the game teaches and the largest unset numbers in Act I, so their economic implication is product design rather than arbitrary fixture tuning. Finite stock and the passerby's limited window complement diminishing marginal value; making selling simply lose to scavenging would make production for sale strictly dominated.
- [ ] **ACT1-16 — Implement tool supersession and reversible effects correctly.** Give replacement families/ranks to Fishing rod → Fishing net and Hatchet → Hand axe. Equipping the better tool replaces action stats without duplicate `SparseSet.add`; physical counts remain sellable; removing the last effective tool restores the correct next-best state. Supersession is family-specific: independent modifiers that merely share a target still stack (for example, sandals and a bicycle may both cheapen Forage). Repair and test the known `health_apply`/`health_remove` asymmetry—apply raises `max` and fills `v`, while remove lowers `max` and only clamps—so the intended lifecycle is explicit rather than falsely assumed to round-trip. Repeated build → sell → build cycles must not accumulate floating-point drift in any apply/remove pair.
- [ ] **ACT1-17 — Wire the Passerby strip and TradeDialog.** BUY/SELL tabs, offer stock, preview, `YOU GIVE` in both directions, live holdings, `TRADE →`, Leave/close/Escape, sold-out state, and modal focus/capture all use authoritative encounter/quote operations. Closing after a successful trade returns focus and refreshes header/Holdings/log in the same frame.

### Act transition

- [ ] **ACT1-18 — Implement a versioned Act I → II migration.** Carry Food unchanged; convert only unspent generic Materials to Biomass `1:1`; preserve goods/counts/effects; remove generic Materials from active Act II accounting; seed Foraging/Woodcutting researched and the remaining ring-1 states per the board spec; initialize population/capacity/market state exactly once. Unit-test idempotence and save-version boundaries.
- [ ] **ACT1-19 — Enforce cross-act cost parity.** Add a catalog test that every same-named recipe/action using Act I Materials and Act II Biomass has identical numeric quantity unless an explicit reviewed exception exists. Garden bed and all migrated recipes participate.

## 10. Act II authoritative backend and shell

Prototype catalogs are deterministic UX fixtures, not permission to hardcode economic outcomes in templates. Stable interfaces may be implemented against fixtures first; live completion requires the following domain work.

### Population, ownership, and catalogs

- [ ] **ACT2-01 — Add population lifecycle and stable identities.** Shelter capacity, sustained surplus, and starvation drive arrivals/departures; every person has stable ID/display label, Vigor, current work, and specialization state. The player remains one agent. Expose current population/capacity as a frame-consistent read. Reuse the capacity already present as `comp.Shelter.capacity`—currently unread beyond Act I's `> 1` gate—rather than introducing a second ceiling.
- [ ] **ACT2-02 — Add per-agent typed ledgers.** Represent Food, Water, Fuel, Metal, Minerals, Biomass, Coin, and goods with stable IDs and ownership transfer APIs. Settlement totals are derived snapshots, not a shared mutable inventory. Generic Materials cannot be earned/spent after migration.
- [ ] **ACT2-03 — Build an authored catalog graph and complete category data.** Every action/material/good/technology has stable identity, act, sector(s), production stage, category/type, recipe, costs, duration, effects, inputs/outputs, and optional research linkage. Inputs distinguish consumed, retained/tooling, upkeep, and produced; consumed intermediates reuse the count semantics already exercised by Act I. Several inputs may converge on one output, and seam goods may name two sectors. The current roster is almost entirely sixteen second-order goods that make food/comfort while nothing makes them; author higher-order stages such as a forge, workbench, worked stone, charcoal, and planks. `src/tags.zig` currently stops at `Food`/`Comfort`/`Tool`/`WoodCutting` and nothing reads those tags, so complete the set as part of this catalog—the STRUCTURE specialization/category model is its first load-bearing consumer.
- [ ] **ACT2-04 — Preserve economic openness.** Production structure is displayed and costed, not a hard dependency lock. Cross-specialization production remains legal at a penalty. Keep `prereq_of` only where a modifier truly requires a target verb. A merchant must remain an alternative, not a key that grants permission to build. This is the Ricardian point of the act: trade should pay even when a person could make everything themselves; forcing trade because an input is impossible would foreclose the comparative-advantage demonstration.
- [ ] **ACT2-05 — Add production/action quotes per agent.** One calculation returns typed deficits, Vigor/energy/time price, expected output distribution, Busy/standing reason, specialization multiplier, and eligibility. ACTIONS, BUILD, STRUCTURE effects, AI decisions, and resolution consume the same quote. Test quote/resolution agreement.
- [ ] **ACT2-06 — Expose a frame-consistent market/agent snapshot.** Selected agent, observable holdings/demands, counterparties, offers, revisions, population, catalogs, research state, and balances must come from one coherent revision while autonomous decisions run.
- [ ] **ACT2-07 — Generalize decide → act.** The split currently exists only in shape: the player is the sole decider and there is no AI decider. AI agents must enumerate and invoke the same labor, consumption, action, build, offer, trade, and specialization operations as the player's UI. UI visibility must never be required for simulation progress.
- [ ] **ACT2-08 — Resolve open demand/specialization/exchange-ratio rules before live wiring.** Specify subjective ordinal demands, quantity-sensitive marginal ranks, observability, comparative-advantage/learning rule, bilateral negotiation mechanism, and autonomous ranking policy in `docs/design.md`; keep this roadmap entry until implemented. Specialization state and the operation that changes it are simulation rules—the UI must not assign a profession merely because a player clicks a colored board wedge. Bilateral offers carry counterparty, give/receive typed bundles and quantities, expiry, revision, and status; counteroffers preserve lineage; acceptance settles atomically across both private ledgers and returns both agents' post-trade holdings. The chosen rule must say whether acceptance follows posted prices, bargaining, or a market mechanism. Do not substitute a shared utility score or hardcode every agent to a UI slider threshold.
- [ ] **ACT2-09 — Record transaction evidence and model money emergence.** Completed trades feed bounded history keyed by good pair with quantity, time, and direction. Displayed ranges derive from evidence, state when evidence is absent, and never become an unexplained global price table. Act I's fixed merchant exchanges use the same transaction-record shape while remaining explicitly quoted trades, not fake market discovery. If a medium of exchange emerges, agents need acceptance of and demand for that good, and settlement must distinguish direct from mediated exchange. The HUD exposes the medium and balance only after the simulation recognizes one; a day/act threshold cannot create a coin counter.

### Act II shell and catalogs

- [ ] **ACT2-10 — Instantiate the shared shell for Act II.** Header order is PO, VI, FO, WA, FU, ME, MI, BI, CO with low-state values; runline shows energy rate, Act II, day; tabs are ACTIONS/BUILD/STRUCTURE; Merchant and Activity strips appear outside STRUCTURE; Holdings/BODY and event-log behavior follow shared rules.
- [ ] **ACT2-11 — Implement the 20-action browser.** Use data records and `ActionTile` in a two-column compact collection. With a blank query, show all 20 unlocked actions in authored order, including `short`/unavailable disabled rows; unlike BUILD, ACTIONS has no default in-reach filter. Query fields/aliases/negation/quotes/wildcard match the prototype. Default sort is `none`; support none/ready/time/energy/name in both directions, with none restoring authored order and disabling Direction. Summary is `20 of 20 actions shown` for the blank canonical fixture and `{visible} of 20` after filtering. Activation is enabled only for ready quotes and updates Activity/log.
- [ ] **ACT2-12 — Implement the scalable recipe browser.** Support at least the 50-record canonical fixture without markup changes. Default sort is reach descending. Blank query shows ready plus reach≥`0.5`, excludes owned, and yields `9 of 50 recipes shown` in the fixture; any nonblank query searches every non-locked record, including owned and distant reach states; `tech` works; a building row remains pinned and visible. Sort none/reach/inputs/time/name and both directions match prototype behavior; `none` restores authored order, resets/disables Direction, and totals always use the full authored catalog (`50`) even when locked records have no row.
- [ ] **ACT2-13 — Synchronize research-linked recipes.** A technology becoming researched changes linked recipe eligibility in the same authoritative transaction/frame, inserts newly reachable rows, logs each first unlock once, and never leaves a stale `needs tech` row.
- [ ] **ACT2-14 — Implement the Coin Merchant.** Shared TradeDialog uses `YOU PAY` for buys, `YOU GIVE` for sells, `BUY →`/`SELL →`, finite stock, Coin/resource/good bundles, refusal reasons, and atomic settlement. Market inventory and selected offer survive unrelated view switches; expiry/departure closes or invalidates safely.
- [ ] **ACT2-15 — Implement Act II Holdings/BODY.** Show typed holdings/counts, act-specific margins/flows, population-relevant Body projections, and correct low/positive semantics. Do not display decorative durability/maintenance meters until those systems exist.
- [ ] **ACT2-16 — Keep event/activity behavior coherent across autonomous updates.** ACTIONS/BUILD log and strips refresh from snapshots without tearing; STRUCTURE hides them visually but still receives events/history. Live announcements do not steal keyboard focus.

### Exchange and Act III identity transition

- [ ] **ACT2-17 — Implement Exchange milestone conditions exactly.** Unlock requires population strictly greater than `500` and at least one researched ring-4 technology. Unlock is distinct from funding. Recipe cost is `80 Biomass + 40 Minerals + 24 Metal`, duration `18d`. Test `500` fails, `501` passes only with ring 4, and every resource deficit independently.
- [ ] **ACT2-18 — Raise the Exchange with the prototype's atomic immediate transition.** Revalidate readiness, deduct the complete bundle once, mark the Exchange standing, open its disclosure, record paid values, set Activity to `Exchange standing · Act III open`, emit one log/result, and dispatch the typed Act II → III transition in the same operation. The displayed `18d` remains authored milestone cost/context; do not introduce an unprototyped countdown or cancellation path without a separately approved design change. Refuse repeat activation.
- [ ] **ACT2-19 — Swap shell identity without replacing the instrument.** Standing Exchange changes act label to Act III, `● MERCHANT` timed strip to permanent `◆ EXCHANGE`, and dialog kicker/headings/note through the identity map while preserving offers/accounting appropriate to the transition. The milestone reports `ACT III OPEN`/standing and cannot be raised twice.
- [ ] **ACT2-20 — Preserve person-level economics through later scale transitions.** Individual decisions remain authoritative throughout the Act II band. If a later aggregate model replaces any of them, it must preserve person-level ownership, subjective ordinal demand, and transaction history as observable economic facts; aggregate totals must never silently become shared inventory.

## 11. STRUCTURE research board

### Pure grid and catalog validation

- [ ] **BOARD-01 — Add a pure `src/grid/` hex module.** Implement axial/cube conversion, pointy-top axial→pixel, pixel→hex rounding, ring distance, six neighbors, rings, exact pure/hybrid classification, vertices, and point-in-hex. It imports no UI/game modules. Export it through the appropriate library/test roots.
  - Test `ring = max(|q|, |r|, |q+r|)`, `6n` cells per ring, 61 cells rings 0–4, round trips, boundaries, and wedge order Food→Water→Fuel→Metal→Minerals→Biomass clockwise from 3 o'clock.
- [ ] **BOARD-02 — Author/validate exactly 60 technology records.** Use `docs/hex_tech_board_spec.md` names, coordinates, rings/eras, pure/hybrid domains, effects, explicit costs, enable links, and stable IDs. Validate uniqueness, ring-coordinate agreement, 6 pure tiles per ring, `6(n−1)` hybrids, only adjacent-resource hybrids, and no Act III coordinates.
- [ ] **BOARD-03 — Validate board visual counts.** Across rings 1–4 there are 24 pure and 36 hybrid technologies, 96 rail paths (24 full + 72 half-domain), and material-dot totals/ring counts from the spec. Fail tests on malformed authored data rather than silently drawing it.

### Rendering and layout

- [ ] **BOARD-04 — Build the clipped charcoal board viewport.** Center the pointy-top board, render one neutral unlabeled ring-0 foundation, six perimeter resource labels, and no wedge backdrop, stage circles, R0/R1–R4 labels, bottom summary, or Act III placeholders.
- [ ] **BOARD-05 — Render technology bodies and progression states.** Locked uses resource-toned fill around 24% with no label/interaction; Available around 66% with label; Researched full opacity with label and dark/resource outline. Selected/focused/outward states use heavier fg/warn strokes without relying on color alone.
- [ ] **BOARD-06 — Render pure/hybrid rails and material markers.** Pure gets one closed six-edge resource rail; hybrid gets two contiguous three-edge rails partitioned by nearest radial resource. Draw domain-first dots plus authored-cost extras according to ring counts. Keep stroke width visually stable during zoom where specified.
- [ ] **BOARD-07 — Keep board layout independent from engine tiling knowledge.** Compute tile offsets/sizes from grid/camera math and use ordinary nodes/geometry. Recompute visible geometry without per-tile SVG rasterization. Clip all paint and hits to the board viewport.

### Progression and detail

- [ ] **BOARD-08 — Implement canonical entry state.** Ring 0 is logically researched but not counted. Foraging and Woodcutting start researched; Spring well, Firepit, Surface ore, Quarry pit start available; exactly Gamekeeping, Net fishery, Pottery works, Loom & ropewalk, and Field cultivation are initially available in ring 2; all other outer tiles are locked. STRUCTURE opens with the cumulative `all` filter active, so locked silhouettes establish the full board shape.
- [ ] **BOARD-09 — Implement adjacency progression.** A locked tile becomes available when at least one strictly lower-ring neighbor is researched. Research can open only immediate outward neighbors. Hybrid entry works from either adjacent wedge. Unit-test unrelated tiles remain unchanged.
- [ ] **BOARD-10 — Implement atomic research.** Revalidate selected tile is Available and every authored resource is held; deduct all or none; mark researched; update only affected neighbors; update header/detail/status/build links/Exchange checks together; log and emit one typed domain result. Coin, Vigor, Population, Knowledge, goods, and generic Materials are never research currency.
- [ ] **BOARD-11 — Build the selected-technology detail pane and live status.** Show name, `RING n · ERA`, pure/hybrid and resource tags, progression state, effect, full cost with held/required values, factual deficits, and what it enables. Available+affordable shows `RESEARCH →`; unaffordable shows `NEEDS …`; researched has no repeat action. Status reports selected name plus researched/available counts and announces newly available names after research. Do not give acquisition advice and do not build an item directly.

### Board interaction state machine

- [ ] **BOARD-12 — Implement selection and tooltip behavior.** Available/Researched tiles select by pointer/keyboard; re-selecting the current tile keeps it selected, and only Escape/explicit clear removes selection. Locked and ring 0 never select/focus. Hover tooltip shows compact name/domain/state, clamps to viewport, and hides during pan/modal interaction.
- [ ] **BOARD-13 — Implement cumulative filter and search.** `researched` shows Researched; `available` adds Available; `all` adds anonymous Locked silhouettes and is the default. Search/filter dim nonmatches without revealing locked labels or interactions. The selected tile and its detail remain selected while visually filtered/search-dimmed; filtering does not silently clear user context.
- [ ] **BOARD-14 — Implement keyboard spatial navigation.** Use a roving focus among interactive tiles; arrows choose the best tile in that screen direction with deterministic distance/tie rules; Enter/Space selects; Escape clears; Tab reaches board controls/detail without traversing 60 cells. Focus survives camera moves and research state changes by stable ID.
- [ ] **BOARD-15 — Implement camera controls.** Persist pan and scale per STRUCTURE session; default `100%`, clamp scale to `75–250%`, buttons step `20%`, reset restores scale `1` and pan `(0,0)`, output announces percentage, and buttons disable at scale bounds. Wheel multiplies scale by `1.12` (or its reciprocal) around the pointer so the world point remains fixed. Match the prototype's unbounded pan unless testing demonstrates a recovery issue; Reset is always the recovery path.
- [ ] **BOARD-16 — Implement pointer/touch pan.** Pressing non-control board space or a tile may begin capture; crossing the 4px logical threshold enters grabbing/pan, updates camera in board coordinates, hides tooltip, and suppresses the release-click for that frame; an unmoved press still selects. Ring 0 can initiate pan. Do not add pan bounds absent an approved usability change.
- [ ] **BOARD-17 — Synchronize STRUCTURE with shell state.** Entering forces Holdings collapsed, hides market/activity/log, and grants recovered height to board/detail. Leaving restores destination view's collapse/scroll state. Expanding Holdings while in STRUCTURE does not rebuild/reset selection, filters, research, or camera.
- [ ] **BOARD-18 — Bridge research to BUILD and Exchange.** Linked recipes update immediately and announce once; ring-4 completion refreshes Exchange unlock; no `window.*` event-bus equivalent or duplicate local state exists.

### Distribution visualization

- [ ] **VIZ-01 — Ship the prototype distribution glyph contract.** Use the five existing normal/Poisson/uniform/exponential/fixed SVG shape assets in action tiles, tint/size them consistently, and expose the shared accessible labels. Preserve p10–p90/yield text; the glyph communicates distribution shape, not a separate success roll. This parity task does not depend on board geometry.

## 12. Exact visual/state parity checklist

- [ ] **PARITY-01 — Global visual grammar.** Dark-only terminal; square corners; regular-weight monospace; tight density; one-pixel lines; accent for interaction/progress; warn for attention/building; danger for shortage/failure/cancel; good for met/ready/standing; no unapproved rounded cards.
- [ ] **PARITY-02 — Focus and disabled states.** Every interactive control has a `1px` warn focus-visible outline with `3px` logical offset or equivalent inset where clipping requires it. Disabled controls use line2/dim and reduced opacity, cannot activate, and retain readable reason text.
- [ ] **PARITY-03 — Overflow.** Terminal/board/dialog clip correctly; catalog/side/detail scroll regions do not paint or hit outside; scrollbar gutter is stable; long single-line cells ellipsize; multiline copy wraps; no hidden control becomes unreachable at narrow widths.
- [ ] **PARITY-04 — Motion.** Holdings, stock label sizing, board state, and opacity transitions match their short functional timing, interrupt cleanly, and snap under reduced motion. Build/research completion comes from simulation state, never a decorative timer.
- [ ] **PARITY-05 — Semantic redundancy.** Locked/available/researched, deficit/met, idle/working/building, selected, and low-stock states are distinguishable by copy/glyph/border/opacity as well as hue.

## 13. Verification and acceptance

### Automated tests

- [ ] **QA-01 — Keep three build gates green.** Run `zig build test-ui`, `zig build test`, and `zig build` after every relevant slice and at final verification. Record failures rather than bypassing a target.
- [ ] **QA-02 — Add pure engine tests.** Cover state initialization/eviction, flexible/min/max sizing, focus registry/pruning, shape hit fall-through, capture lifecycle, event consumption, resize invalidation, clipping, and stable keys under reorder.
- [ ] **QA-03 — Add foundation tests.** Cover query parser, UTF-8 editing, wrapped measurement, ellipsis, scroll clamping/reset/persistence, slider math/input, popup focus, modal capture/focus restoration, animation interpolation/reduced motion, geometry generation, and texture eviction.
- [ ] **QA-04 — Add Act I domain tests.** Cover BODY projections, absolute Shelter threshold, eligibility announcements, quote revision/refusals, diminishing marginal prices, atomic barter, supersession, modifier round trips, build lifecycle, migration idempotence, and Materials→Biomass cost parity.
- [ ] **QA-05 — Add Act II domain tests.** Cover typed transfer atomicity/ownership, population boundaries, quote-resolution equality, research adjacency/payment, technology↔recipe links, Merchant settlement, Exchange `500/501` boundary, Exchange payment/completion, and one-time Act III transition.
- [ ] **QA-06 — Add grid/catalog invariant tests.** Cover all BOARD-01..03 counts/classification, canonical opening state, resource vocabulary, unique IDs/coordinates, no generic Materials research costs, and no unavailable resource type without an authored acquisition route.

### Deterministic UI fixture harness

- [ ] **QA-07 — Add a development-only fixture entry point.** Select named screen/state via CLI or compile-time dev menu, not production URL logic. Fixtures provide deterministic window size, time, catalogs, balances, focus/hover/selection where needed, and seeded domain snapshots while still exercising production templates.
- [ ] **QA-08 — Support scripted input replay.** Feed synthetic pointer/key/text/wheel events into the host input model without SDL polling so interaction state machines can be regression-tested. Keep pixel rendering tests separate from pure input/layout assertions.
- [ ] **QA-09 — Add a node/layout debug dump.** For a fixture, emit stable IDs, rects, clips, focus/selection, render features, and semantic metadata. Use it to diagnose overlap/clipping and to assert hidden/locked nodes are not focusable/hittable.

### Screenshot matrix

Capture at the reference `900×820` logical size and compare to the current HTML/screenshots. Also capture `760`, `560`, and `440` width classes plus one high-DPI scale.

- [ ] **QA-10 — Act I screenshots.** ACTIONS; BUILD default; BUILD query/sort; active build/cancel; Holdings collapsed; Shelter locked/unfunded/ready/details; Passerby BUY; Passerby SELL; low-stock/disabled states; narrow layouts.
- [ ] **QA-11 — Act II shell/catalog screenshots.** ACTIONS default/filtered; BUILD default/all-filtered/active build; Merchant BUY/SELL/sold-out/insufficient Coin; Holdings expanded/collapsed; Exchange locked/unfunded/ready/details/standing; Act III identity.
- [ ] **QA-12 — STRUCTURE screenshots.** Canonical opening; researched/available/all filters; search dimming; selected available affordable; selected deficit; selected researched; outward unlock; tooltip; 75/100/250% camera; panned board; keyboard focus; expanded Holdings; narrow stacked board/detail.
- [ ] **QA-13 — Overlay screenshots.** Modal backdrop and clipping, long offer copy wrapping, sort popup near edges, focus-visible states, reduced-motion terminal state, and terminal centered in an oversized window.

### Interaction journeys

- [ ] **QA-14 — Keyboard-only Act I journey.** Navigate tabs, change eating rate, start/cancel build, inspect/raise Shelter, open/switch/select/complete/close trade, and return focus correctly without a pointer.
- [ ] **QA-15 — Keyboard-only Act II journey.** Search/sort actions and recipes, trade, open milestone details, enter STRUCTURE, change filters/search/zoom, navigate/select/research a tile, clear selection, and leave with view state preserved.
- [ ] **QA-16 — Pointer/touch conflict tests.** Verify nested cancel does not start, modal blocks background, drag suppresses click, wheel targets the hovered scroll/board, resize removes stale hits, tooltip does not block, and clipped rows/tiles cannot activate.

### Performance and resource acceptance

- [ ] **QA-17 — Profile representative worst cases.** At minimum: 20 actions, 50 recipes, open dialog, and all 61 board cells with rails/markers at 250%. Maintain the application's frame target on the reference machine without per-frame SVG loads or unbounded arena/pool growth.
- [ ] **QA-18 — Run leak/lifecycle checks.** Repeatedly switch tabs, filter catalogs, open/close overlays, resize, and enter/leave STRUCTURE; verify text/SVG/geometry textures and owned buffers evict, pointer capture/focus clears, and stale slots do not remain hittable.
- [ ] **QA-19 — Verify copy/data provenance.** Every displayed balance, requirement, deficit, duration, progress, stock count, effect, and readiness comes from one authoritative snapshot/catalog/quote. Search the implementation for duplicated prototype fixture numbers outside fixture/catalog data.

## 14. Cleanup after parity

- [ ] **CLEAN-01 — Retire `src/ui_client/widgets.zig` only after replacements land.** Remove duplicate button/panel/scroll/modal/tooltip/text-input implementations and barrel re-exports; keep one foundation/template path.
- [ ] **CLEAN-02 — Remove superseded BUILD state/types.** Delete chip-filter `BuildViewState` fields, `Kind.blocked`, blocked-row copy/styles, old tier/effect filtering, and stale “good” terminology. Keep `GoodT` as a domain type name only if technically useful; player-facing text says recipe/good according to context.
- [ ] **CLEAN-03 — Remove static distribution assets only when no live/reference consumer remains.** Keep prototype files/screenshots as design fixtures; production should use generated curves only after DEFER-09 is approved, implemented, and every live consumer has migrated.
- [ ] **CLEAN-04 — Remove prototype-only global event patterns from production design.** No equivalents of `window.prototype*`, DOM CustomEvents, hash routing, or query-parameter state in game code. Use typed domain results, session UI state, and the fixture harness.
- [ ] **CLEAN-05 — Reconcile stale comments/docs.** Remove claims that full tests are red if current commands pass, old direct-barter Act II references, fixed 64-byte assumptions after text work, and obsolete input-capture caveats after ordered/captured input ships.
- [ ] **CLEAN-06 — Audit accessibility labels and visible copy.** Ensure abbreviations have full names, distribution glyphs have labels, icon-only controls have names, live regions do not spam, and old screenshot vocabulary cannot reappear from duplicate strings.

## 15. Explicit non-goals and deferred work

These remain in this file because it is the only future-work document, but they do not block Act I/II prototype parity unless a measured dependency proves otherwise. Act III's economy is deliberately unspecified beyond the sketch in `design.md`: firms, deeper division of labor, and a global source of capital goods the city buys from. That content cannot be designed honestly before Act II's demand/negotiation model in ACT2-08 exists; opening Act III identity in ACT2-18/19 does not pretend otherwise.

- [ ] **DEFER-01 — Pause/menu overlay.** Reuse the completed modal foundation. Pause is deliberately unresolved for economic, not architectural, reasons: dropping `Busy` already leaves the body doing exactly one thing, but pausing a long build would let the player chip at a hatchet between forage runs until the manufactured tier becomes reachable by attrition, removing pressure the passerby exists to relieve. Cancellation itself already exists as `capital.cancel_build`. Define that policy before adding pause. A menu may expose only values explicitly marked player-configurable, with validation, ranges, and reset; raw mutable access to all of `res.config` would turn tuning internals into accidental UI API. Decide persistence before promising it.
- [ ] **DEFER-02 — Capital durability/maintenance.** Goods are currently permanent once built; nothing wears. Add authoritative slow decay, condition, and upkeep before Holdings or a board tile displays a meter. Do not ship decorative wear UI.
- [ ] **DEFER-03 — Audio/music.** Add ambient music only after core UI/simulation lifecycle provides volume/configuration ownership.
- [ ] **DEFER-04 — Act III board thirds.** A later site may split one hex into three rhombi meeting at its center. This should cost the engine no new tiling concept—the same tile geometry, offset placement, and host-supplied hit predicate can compose it—so preserve APIs that allow adoption without a rewrite, but do not render or reserve thirds in Act II.
- [ ] **DEFER-05 — SparseSet scale policy.** `world.zig`'s `SparseSet(T)` currently allocates three `[MAX_ENTITIES]` arrays (`dense_ids`, `dense_values`, and `sparse`) per component type, so memory cost is `num_types × MAX_ENTITIES`, not occupancy. Entity IDs are never recycled, making `MAX_ENTITIES` a lifetime-spawn cap rather than a live-population cap. This becomes material when the capital roster reaches hundreds or the ID ceiling must grow for population—not merely when there are hundreds of component types. Size dense arrays to occupancy; use a hashmap-backed sparse index for rarely carried types while hot types such as `Vigor` and `InventoryFood` retain flat indexing. Choose the per-type policy where `Storages(ns)` constructs each storage. Keep the change contained to `world.zig` plus `ecs.zig`, whose `Query` driver must stop reading `.dense_ids`, `.dense_values`, and `.len` directly and use storage methods.
- [ ] **DEFER-06 — Additional sizing combinators.** Add `range`/`max_of`, `stretch`/`align-content`, and—only if measured compositions require it—a `strictness: f32` violation pass that distributes slack/overflow among siblings. Do not pre-build web layout breadth when UIE-03 expresses the concrete responsive layouts.
- [ ] **DEFER-07 — O(interactive) stamping.** Profile before attempting. The event stage is already O(interactive) because `mark` iterates live slots without a tree walk; `stamp_rects` is only one inexpensive O(all) pass among sizing, percentage resolution, placement, stamping, and drawing. Z-ordered hit testing does not retire it: the paint-order list must still be produced by this walk because query order is not paint order.
- [ ] **DEFER-08 — ZLS query tuple ergonomics.** `q.iter().next()` returns an `@Type`-constructed tuple that ZLS does not evaluate, so multi-fetch destructuring currently needs explicit annotations. Declaring `Query` parameters as concrete `[]const type` instead of `anytype` was measured and changes nothing; the limit is `@Type`. Complete type visibility only if ZLS's comptime interpreter gains that support.
- [ ] **DEFER-09 — Parameterized distribution curves.** If playtesting needs the mini-curve to encode an action's actual p10–p90 parameters rather than only its distribution family, generate it through the geometry feature and replace the five static parity SVGs. Treat that as a reviewed extension to the finalized prototype, not a board prerequisite.

## Definition of done

Act I/II implementation is complete only when all non-deferred checklist items are checked and evidenced:

1. The production app reproduces the prototype shell, visual states, responsive layouts, and interaction state machines without HTML/DOM runtime code.
2. Every shared instrument is one reusable Zig component with act-specific data/vocabulary, not parallel copies.
3. All changing values and atomic operations are backed by authoritative simulation/catalog/quote state; fixtures exist only in the development harness.
4. Pointer, keyboard, focus, modal capture, scrolling, slider, catalog, and board-camera interactions pass automated journeys and visual review.
5. Act I transitions safely into Act II with the exact resource/research migration invariants; Act II can raise Exchange and switch to Act III identity exactly once.
6. `zig build test-ui`, `zig build test`, and `zig build` are green; required screenshot matrices and leak/performance checks are recorded; no competing future-work document or escaped code TODO remains.
